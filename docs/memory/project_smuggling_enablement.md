---
name: project_smuggling_enablement
description: "Making mission-learn workers eligible for category=smuggling missions: the treasure_cache tutorial chain, the smuggling-level-2 courier gate, mission-capture infra, and the 3-part plan (capture procedural missions / enable category / level-2 bootstrap)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 3d5d5416-b3fa-4111-ad22-3c6bfb554e8f
  modified: 2026-07-30T02:15:06.269Z
---

Goal: every mission-learn worker becomes eligible for `category=smuggling` couriers and runs them autonomously. Eligibility is a GAME skill gate, not just our client allowlist.

**⭐✅ 2026-07-29 (evening) — THE WHOLE LADDER RAN FROM LEVEL 0, AUTONOMOUSLY, AND IT REPLICATES 3/3.** Three fresh canaries (fighter-2 1.55M / explorer-2 133k / explorer-1 15k, all pinned to `treasure_cache_trading_post`, all smuggling L0) each cleared chain 1, and **explorer-2 carried it all the way to the pirate-baseline unlock**: `19:02:46 accept` → `19:03:03 running 1 mission to alhena (5 jumps)` → `19:08:20 Arrived at voss_redoubt` (docked at the stronghold, unattacked) → `19:08:50 completed an_introduction +2867 cr`. It then read `smuggling_run_voss_redoubt_station_*` from INSIDE the pirate network. engineer-1's earlier proof started at L2 with a hand-placed pin, so the from-zero path was untested until now.

- **THE L3 GATE ON `an_introduction` IS NOW EMPIRICALLY CONFIRMED** (it was already recorded further down this file from the operator, 2026-07-25 — this is confirmation, not a new discovery). At L2 all three stood AT the giver and logged "no acceptable missions" with **no skip line for `an_introduction`** — i.e. not offered, not offered-and-rejected. After one 27-jump courier took them to ~600 XP (L3, threshold 565), explorer-2 accepted it at that same station within minutes. Sequence per agent: `no_questions_asked` (+100) → `across_the_line` (+200) → `a_word_in_private` (+50) → one courier (+250) → **L3** → `an_introduction`.
- **CREDITS ARE NOT THE CONSTRAINT — reachability and fuel are.** explorer-1 started on 15,135 cr (deliberately matched to explorer-12's 15,953) and did exactly what the 1.55M fighter-2 did. Pick canaries ≤2 jumps from the giver; that alone avoids the engineer-5 strand.
- **Payout variance on ONE mission id spans the full range and does not track wealth:** `across_the_line` paid 2500 / 577 / 126 to the three; `an_introduction` paid explorer-2 **+2867** where engineer-1 got **+0**. Never budget a chain run at board value.
- **THE PIN RELIABLY REELS THEM BACK, even from 17–27 jumps out** — the 3-dry-pass counter fired for all five canaries and none stalled. An earlier worry in-session that marginal local work would defer the return indefinitely was WRONG. But note each chain link is a long round trip (17 jumps for leg 2, 27 for the L3 courier), so from-zero chain 1 is slow, not cheap.
- **TWO GATE DEFECTS FIXED (`d6a739f`), both found by watching these canaries:** (1) the smuggling-XP gate credit keyed on `e.Type`, so `a_word_in_private` — **delivery-typed but paying 50 smuggling XP** — got no credit and was refused at net 493 vs the 500 floor, over 7 credits, while being 83% of the L0→L1 requirement. Now keyed on what the mission PAYS; for non-smuggling-typed entries the credit may only rescue a mission already in credit profit, because negative-net XP buys are budgeted on the smuggling branch alone and 1250 cr of credit would otherwise drag a net as low as −750 past the floor unbudgeted. (2) A refused accept was dropped from the trip WITHOUT `markAttempted`, so the next pass re-priced and re-accepted it — three canaries re-accepted one L1-gated courier every ~10s indefinitely (12 failures in 4 min). **Consequence to be aware of:** couriers attempted while under-levelled are now retired for the session, so an L0 agent burns them just before reaching L1 — see the pre-accept-level-gate follow-up.
- `a_word_in_private` at **0 jumps nets exactly 500 = the floor**, so it passes with or without the XP fix. Confirming that fix live needs a worker seeing it from ≥1 jump away; still unverified in production.

**⭐✅ 2026-07-29 — `an_introduction` RUN AUTONOMOUSLY END TO END. The pirate-baseline gate is BEATEN, and the route is repeatable.** engineer-1 accepted it 34s after relaunch and completed it 6 minutes later: `02:22:15 accept` → `02:22:33 running 1 mission to alhena (5 jumps)` → `02:27:50 Arrived at voss_redoubt` → `02:28:21 completed`. It **docked at a pirate stronghold at hull 420/420, unattacked**, with standing still hostile — the one-time chain passage is real.

- **PROOF OF UNLOCK is `supply_run` appearing on the board 4 min later**, not the reward line. supply_run is chain 2's first real leg and only boards once `an_introduction` is done. Don't look for a credits or standings signal (see the two traps below).
- **Three gates had to fall, and shipping only the obvious one would have changed nothing** (`21e7e9f`): (1) `deliverShape` took `len(Objectives)==1`; (2) **the stronghold guard would have refused it anyway** — an_introduction DELIVERS to voss_redoubt/alhena, which is in `systems.is_stronghold`. Chain missions (`chain_next` non-empty; procedural couriers leave it empty) may now have a stronghold as their own DESTINATION, road still checked; (3) the resume path was gated on one objective too. Route treasure_cache→alhena is 5 jumps and stronghold-clear either way the server routes it.
- **🔴 THE WORKER STILL THINKS IT IS LOCKED.** The next trip line was still XP-led, so `smugglingUnlocked` reads `baseline < 10` — almost certainly STALE LOCAL STANDINGS (logged in at baseline −30; nothing since carried a standings payload, and the preserve-across-partial-payloads behaviour keeps the old value). Consequence is real: it keeps buying XP it no longer needs. **Needs an explicit standings re-read after a chain completion.**
- **🔴 `an_introduction` wrote NO `mission_results` row** (0 rows match). The in-game mission completed but telemetry lost it, so the chain completion is absent from the dataset. Unknown whether that is specific to multi-item candidates or to chain missions — check before the next agent runs it.
- **Paid 0 of 3000** (engineer-2 got 1605 for the same mission). This is the cleanest kill yet for the elapsed/budget reward model: delivered in 5 min against a ~14-DAY window — the earliest possible delivery, ratio ≈0.0003 — and it paid nothing. See [[project_mission_learning_pool]].
- **🔴 CHAIN 2 IS BLOCKED BY A DIFFERENT WALL:** `supply_run` has `provided_items = {}` and needs **10 ghost_rounds sourced**, refused by the (correct) contraband rule since contraband has no market listings. Chain 2 needs a real acquisition path (craft/loot), not a gate change.
- **🔴 A LEVEL-0 AGENT COULD NOT START AT ALL — fixed in `1e8e6bb`.** Every smuggling-TYPED mission needs L1 already, so the only XP a fresh agent can earn is **`a_word_in_private`**, which is **delivery-typed with a `dock_at_base` objective, NOT deliver_item** — `deliverShape` refused it. engineer-1 never exposed this because it was already L2. dock_at_base is now a delivery that carries nothing. Two non-obvious co-changes: a held visit owes no cargo (must not be abandoned as `cargo_lost`), and folding a dock objective into `activeMatchesItems` added an empty-item key that would stop a visit ever matching itself. `a_word_in_private` boards at **13 stations**, incl. treasure_cache and grand_exchange.
- **CANARY 3 = engineer-5** (pinned + smuggling, `1e8e6bb`): the from-zero repeatability test engineer-1 never ran. **Chosen for RUNWAY: explorer-12 (nominal canary 2) holds ~16k credits against the 25k XP-loss budget and the climb costs ~11k/night, so it would go broke mid-chain.** engineer-5 has ~702k / 935 cargo.
- **OPS: cycle ONE worker onto a new binary via the overrides sidecar** (add to `removed[]` → SIGHUP → **wait for the removal to COMPLETE** → restore → SIGHUP). The supervisor treats a re-add during an in-flight removal as a CANCEL, so a fast second SIGHUP silently leaves it on the old binary. A `station:`/category edit alone is enough — a changed spec is a `MembershipUpdate` that drains and relaunches that one worker. Beats restarting all 37.
- ⚠️ `mission-learn-overmind.log` hit **2.09 GB** (was 893MB); nothing rotates it and it shares a disk with market.db.

**⭐ 2026-07-28 — engineer-1 REPINNED to `treasure_cache_trading_post` (committed), and the ONE remaining blocker is multi-objective support.** The canary climbed to smuggling **L5**, so the pin's job changed from "sit where couriers post" to "reach the chain giver". Sequence and findings:

- **Location settled from the DB without ever having captured the mission.** `an_introduction` is STILL absent from `mission_templates` (engineer-2 ran it via play_as pre-capture), but its chain neighbours pin it: `across_the_line` (`chain_next=an_introduction`) and `supply_run` both appear in `mission_template_locations` at `base_id=treasure_cache_trading_post` / `system_id=treasure_cache` **and nowhere else**. Operator confirms it is offered only there. New link recorded: **`supply_run.chain_next = expanding_operations`**.
- **🔴 THE RUNNER CANNOT ACCEPT IT EVEN PARKED AT THE GIVER.** `deliverShape` (`pkg/worker/mission_select.go:191`) admits `len(e.Objectives) != 1` → reject; `an_introduction` carries TWO `deliver_item` objectives (10× starshine + 5× nerve_burn → voss_redoubt/alhena). It will skip as **"not a plain deliver mission"** — the same reason already logged ~17×/day. The pin buys POSITION, not CAPABILITY. Unblock = manual `play_as engineer-1` (proven route, engineer-2 did exactly this) **or** build multi-objective support (the one that scales to the operator's cycle-every-agent plan). [[project_mission_learning_pool]]
- **⚠ REPIN WHEN THE AGENT IS DOCKED, NOT MID-FLIGHT.** `station:` is a **CLI flag**, so a yaml edit + SIGHUP issues a `MembershipUpdate` → `memberRemove(relaunch)` → drain, then **force-stop after `RemoveDrainTimeout` = 4 min** (`pkg/overmind/supervisor/supervisor.go:175`). Restarting engineer-1 mid-route stopped it at **Rasalgethi, which has no station**, so it backtracked 3 jumps to The Rampart to dock (stranded-recovery worked — it docked rather than drifting, so no [[reference_gsa_ship_recovery]] fee). Cost ≈ **4 extra jumps**: it was already several jumps along the route to Haven, and **Haven is only 3 jumps from treasure_cache** while Ironhearth is 27 — so once it had departed, letting it finish that leg was strictly better. Check the agent's ACTUAL position before firing, not where its mission ended.
- SIGHUP safety confirmed: `diffSpecs` emits `MembershipUpdate` on any changed field (`reflect.DeepEqual`), and `safeDiff` refuses an empty roster or >half-removal. A one-line station edit = 1 update / 0 removes, which clears the guard.
- **FIRST REWARD-CURVE ROW (the `expiry_budget_ticks` instrumentation works end-to-end):** `budget=1031, elapsed=485, ratio=0.47, expected=2000, paid=2000` (26 jumps, ironhearth nerve_burn courier). Delivered at 47% of window → FULL reward. One point only — consistent with the operator's `elapsed/budget` model but the informative rows are those past ~0.8. The XP-led trip line also printed live: `(26 jumps, +250 XP; credits est 544 but a late delivery may pay 0)`.

**✅ UNBLOCKED + SHIPPED 2026-07-27 (`1109570`), fleet restarted on it.** Live levels that day: **engineer-1 = L2 (88/340)**, **engineer-2 = L4 (639/900)**, so the L2 courier gate is CLEARED for both and chain 1 is NOT needed again. The only blocker was our own net floor: real boards price couriers at −680…−2312 and `missionMinNet`=500 rejected every one, so the canaries made **zero accepts — no evidence either way about the worker binary**. Fix: `effectiveMissionFloor` relaxes smuggling to `missionSmugglingXPFloor`=−2500 while a per-worker `missionSmugglingXPBudget`=25000 of realized LOSS holds (profitable runs cost no budget; loss booked only after the server grants the run). **⚠ The `station:` field was INERT for missionrunners until `bdd1104`** — `WorkerDispatch.Station` was read only by the assist role (`ensureHome`), never threaded into `MissionDeps`, so a missionrunner ignored it and toured the nearby-station pool on every dry streak. The flag reaching the process proves NOTHING about the role honoring it; verify with the `returning to pinned station <id>` log line. Fixed by adding `MissionDeps.HomeStation`: a dry pass parks when already docked at the pin, otherwise routes home; empty keeps the old roaming. Use the **base id** (`grand_exchange_station`), not the POI id (`grand_exchange`) — the yamls contain both spellings for different roles. Both canaries also **pinned to `grand_exchange_station`** — roaming was silently choosing their economics (engineer-1 was idling in Dheneb at a board-less POI while a 1338-tick courier sat unseen on Grand Exchange; explorer-12 had drifted to Alpha Centauri with zero smuggling sightings). Economics: ~75–100 XP per courier at ~2000–2300 cr, so a level ≈ 3 couriers ≈ 6,600 cr — engineer-1 holds 499,850 cr.

**⭐ 2026-07-27 (evening) — THE PIN TOOK THREE FIXES, AND THE BINDING GATE IS NOW EXPIRY, NOT NET.** The `station:` pin failed at three separate layers, each of which looked fixed from the layer above: (1) `WorkerDispatch.Station` never threaded into `MissionDeps` (`bdd1104`); (2) the park branch compared `st.Ship.DockedAtBase`, but that field is on the **player** — the test set the ship copy too, so it was green against a payload the server never sends (`8618685`); (3) the client never recorded the base on dock at all, so the player copy was `""` too (`d7c01c3`). See [[reference_docked_at_base_gotcha]]. **explorer-12 now parks; engineer-1 still reads `docked_at=""`** because its dock completes via `TypeDocked`/`auto_dock`, which carry no base id — it re-docks each dry pass (fuel cost only; board polling is unaffected).

**Board reality, corrected:** an in-session claim that "Grand Exchange only ever posts `smuggling_black_*`, couriers are all issued by confederacy_central_command" was **WRONG** — it came from a sample window that ended minutes too early. Grand Exchange posted `smuggling_courier_grand_exchange_station_confederacy_central_command_hot_cell` at 19:32Z. The pin station is fine. (Courier ids read `smuggling_courier_<issuer>_<destination>_<item>`.)

**Current binding gate = the expiry margin, and it missed by ONE tick:** `expires in 197 ticks (< 198 needed for 14 jumps)` = `missionSmugglingMinExpiryTicks`(30) + 14 × `missionTicksPerJump`(12). The already-relaxed smuggling cushion is what refused it; the travel allowance alone (168) fit inside the 197 available. Lever = `missionSmugglingMinExpiryTicks` (mission_select.go:45); ~12 would have admitted this run with 17 ticks spare. **Weigh against v0.549.0 (late delivery beats defaulting) before relaxing — that note was written about FREIGHT and has NOT been verified for smuggling** [[reference_v0549_freight_and_percrew_pirates]]. Note this is still the tier-0/1-hull problem in another costume: a cheap hull would not change expiry, but the operator's hull lever (below, dated entry) remains the higher-value fix.

**▶ NEXT (operator plan 2026-07-27): once the canaries reach L3, complete chain-2 mission 1, and gain the pirate reputation, CYCLE EVERY OTHER AGENT through the same flow.** The canary is proving the worker binary can climb autonomously; if it does, this becomes a fleet-wide rollout rather than a per-agent manual play_as job. Sequence per agent: pin to a capital-system station → relax floor → couriers to L3 → chain 2 mission 1 → reputation. **Operator model: every EMPIRE station should carry some smuggling missions, with capital systems (Sol, Haven, …) carrying the most.** ⚠ Our logs only ever show smuggling boards at 3 issuing stations (treasure_cache, grand_exchange, confederacy_central_command) despite engineer-1 docking at 8+ — but those counts come from DEDUPED skip logs (`shouldLogSkip`), so they understate coverage. Verify against a real board before concluding anything is filtered.

**🔴 PRIOR STATE — THE COURIER CANARIES WERE DEADLOCKED (superseded by the entry above).** engineer-1 and explorer-12 have completed **ZERO** smuggling missions — ever. **Smuggling level is PER AGENT**, and chain 1 was run by **engineer-2**, so engineer-1 inherits nothing: every courier accept died on `skill_required: Courier runs require smuggling level 2` (07-26 13:11–13:12). Since then the missionrunner never even tries — couriers are pre-skipped on economics (`net -1988` to `-2312`, floor 500) or on expiry (`expires in 42 ticks < 270 needed for 20 jumps`). The only entry-tier alternative, `smuggling_black_*` Black Market runs, is skipped 8× with **"smuggling run must source N x <contraband> itself; contraband is not sold on the market"** — the worker has no sourcing path (contraband comes from loot/salvage, not markets). explorer-12 sees no smuggling board at all from alpha_centauri.

**To unblock, engineer-1 must run chain 1 ITSELF** (`no_questions_asked` → `across_the_line`, Dara Kesh @ treasure_cache_trading_post, items provided) exactly as engineer-2 did — that is the only route to L2. Same for every future courier. Note the XP pricing is also unstable on identical missions: the Starshine→Frontier run priced `+XP = 302` at 14:52 and `+XP = -946` at 15:40 — worth a look before trusting the XP floor. [[project_mission_learning_pool]]

**PHASE 1 CONFIRMED end-to-end (engineer-2, 2026-07-25):** both chain-1 missions (incl. the cross-border Sol run, clean on a freighter) → level 2 → smuggling courier accepted (was skill_required at L1). Accepted courier instance id = `smuggling_courier_claim_<hash>`; courier expiry varies wildly (87 ticks @ treasure_cache vs ~11h @ Sol). Couriers spawn fleet-wide (seen on Sol board too, not just treasure_cache). Still pending: the `an_introduction` (chain-2 entry) shape + chain 2/3 shapes.

**Pilot (2026-07-25, engineer-2 @ treasure_cache_trading_post, driven via play_as):**
- **Eligibility gate:** repeatable smuggling courier runs require **smuggling level 2**. Accept at level 1 returns `{"code":"skill_required","message":"Courier runs require smuggling level 2."}` — the `code:"skill_required"` is the programmatic bootstrap trigger.
- **CHAIN 1 IS EXACTLY 2 MISSIONS** (operator correction 2026-07-25, verified against `mission_templates`), anchored at treasure_cache_trading_post, Nebula; giver **Dara Kesh**; both single `deliver_item` with items PROVIDED (no buying):
  1. `no_questions_asked` (type smuggling, diff2, 5 starshine→Haven, Nebula-internal) → +100 smuggling, 800cr; chain_next `across_the_line`
  2. `across_the_line` (type smuggling, diff4, 3 void_dust→Sol/confederacy_central_command, **CROSSES Solarian border, 17 jumps**) → +200 smuggling, 2500cr; chain_next `an_introduction`
- **`a_word_in_private` is NOT part of chain 1.** Giver is **Dockworker Brix** (not Dara Kesh), `type=delivery`, and its `chain_next` is EMPTY — a standalone intro delivery that happens to pay +50 smuggling (+35 nebula_attunement), 500cr. Earlier notes miscounted it as "leg 1 of 4"; it is not a leg at all.
- **`an_introduction` = the ENTRY TO CHAIN 2, not a leg 3 of chain 1.** It is `across_the_line`'s `chain_next`. **Unlock = chain 1 complete AND smuggling L3** (L3 comes from a few repeatable couriers after chain 1 — operator confirmed 2026-07-25). SHAPE (operator-supplied 2026-07-25, board text; not yet in `mission_templates` — capture on sight):
  - "An Introduction" — Chain Mission, giver Dara Kesh, difficulty **5/10**. Flavor: Kesh introduces you to her contacts in pirate space, "a gift that will open doors — literally."
  - **TWO objectives, both `deliver_item` to `voss_redoubt` at ALHENA (a PIRATE STRONGHOLD):** 10× `starshine`, 5× `nerve_burn`.
  - Rewards: **3000 cr, pirate +2 rep, smuggling +250 XP.**
  - **This is the first MULTI-OBJECTIVE mission in the chain** (chain 1's two were each a single `deliver_item`) → the missionrunner needs multi-objective support before it can run this autonomously; same gap already flagged for exploration missions in [[project_mission_learning_pool]].
  - **Stronghold mapping filled in: `voss_redoubt` lives in system `alhena`** — which pairs with the created-but-unfleeted `marketbot_alhena` account, so that's its intended resident stronghold. Closes one of the 4 TBD stronghold↔marketbot pairings. [[reference_pirate_base_registry]]
  - **Items ARE PROVIDED** (operator-confirmed 2026-07-25), same as chain 1 — no buy/sourcing step. The 10× `starshine` + 5× `nerve_burn` are handed over on accept, so the run is a pure 2-objective delivery. Cargo needed ≈ 15 units.
  - **✅ COMPLETED on engineer-2 2026-07-25.** Completion text is the key finding: *"You now have enough standing with the pirate network to dock at their strongholds freely."* → **`an_introduction` ALONE grants permanent free docking at pirate strongholds.** This CORRECTS the earlier model that stronghold access required finishing chain 2. **Actual rewards paid: +1605 cr and smuggling +250 XP** — note the credits came in at **1605, not the advertised 3000** (53.5% of board value; cause unknown — scaling, cut, or an advertised-vs-paid gap. Do NOT budget chain rewards at board value).

    **⚠ STRONGHOLD ACCESS = REPUTATION, and the mission raises the BASELINE (operator-corrected 2026-07-26).** engineer-2's post-completion standing reads `"pirates": {"baseline": 10, "outstanding_bounty": 0, "reputation": 10}`. An earlier version of this note read that as "baseline 10 is the universal default, so rep is not the signal" — **that inference was WRONG**. The model, per the operator with live data:
    - **Default for EVERY agent is `baseline: -30, reputation: -30`** (hostile). The old "pirate rep −30" premise in these notes was right after all.
    - **Completing chain-2 mission 1 (`an_introduction`) raises the BASELINE −30 → 10.** That is the permanent unlock; engineer-2's `baseline: 10` reading was its POST-completion state, not the default.
    - **`reputation` — not baseline, not the mission record — is the SERVER-SIDE stronghold gate.** Reputation floats above baseline from missions/shuttling and **decays back toward baseline** when idle. Raising the baseline is what keeps it permanently above the dock threshold.
    - Live confirmation: craftsman-1 reads `pirates 29 (base 10)` — unlocked, currently 19 above its raised baseline.
    - **🔴 SUPERSEDED 2026-07-26 by v0.549.0: PIRATE STANDING IS NOW PER-CREW, not one `pirates` faction.** Docking is gated on standing with **the crew that runs that station**, and *"being barred from one stronghold says nothing about the next"* — so a single `pirates.reputation` number is no longer the gate, and the single-threshold model just below is obsolete as stated. Supplying a stronghold, running its cargo, or moving a crew's contraband now earns goodwill with **that crew specifically**, and **missions state which crew their pirate standing pays**. `an_introduction`'s network-wide introduction SURVIVES (still "an introduction to the whole network at once"), so the bootstrap unlock is intact — but **Phase-2 stronghold residency now needs per-crew standing per target stronghold**, and the unbuilt dockability gate must be keyed on the target station's crew, not a global number. Pirates also now report `faction`/`faction_name` and the `get_nearby`/`get_state` pirate tables gained a `crew` column (our JSON-by-key decoding is unaffected, but the structs drop the new fields). [[reference_v0549_freight_and_percrew_pirates]]
    - **THRESHOLD (operator, 2026-07-26, PRE-v0.549.0 single-faction model): a pirate stronghold station requires `pirates.reputation >= 10`.** Baseline 10 therefore means the gate is permanently satisfied, since rep decays only as far as baseline — that is exactly why raising the baseline IS the unlock.
    - **SECOND ACCESS PATH: the one-time chain-mission grant.** An accepted chain mission targeting a stronghold carries its own passage for that delivery, so rep < 10 is not disqualifying for it. **Critical for gate design: `an_introduction`'s own destination IS a stronghold (voss_redoubt/alhena), so a blanket stronghold skip permanently blocks the one mission that grants stronghold access.** The gate must allow a mission that carries its own grant.
    - **WIRE SHAPE CONFIRMED** (live: `data/game-api/latest/get_status.json` → `player.standings`; openapi `EmpireStanding`): `map[empireID]{reputation, baseline, outstanding_bounty, jailed_until?}`, rep range −100..+100, baseline documented "Decay target; rep drifts toward this over time". Live defaults: pirates −30/−30 (**the only hostile faction**), solarian 20/20, nebula/crimson/outerrim/voidborn 10/10.
    - **Client gap: player standings are NOT MODELLED AT ALL.** No `baseline` / `reputation` / `outstanding_bounty` block anywhere in pkg/game (only `MissionRewards.PirateRep`, a reward delta, and `ShipClass.RequiredReputation`). Any gate needs the standings block captured onto State first. The +250 XP takes engineer-2 from 417/585 to 667 → **smuggling L4**. **Chain continues: `supply_run`** (accept_mission), and Kesh teases "real work… the kind that pays properly" = chain 2 proper.
  - **ACCEPTED on engineer-2 2026-07-25T18:56:48Z** — instance id `df77b4f8a9e8cd278a85b9ddd3269788` (this hash, not `an_introduction`, is what `complete_mission`/`abandon_mission` take). **Expiry 2026-08-08T18:36:18Z = ~14 DAYS** — far longer than the couriers' 87-tick/11h windows, so there is no time pressure on the Alhena run.
  - **Board vs accepted view render differently BY DESIGN — not a bug** (traced 2026-07-25). The board formatter (`main.go` ~4785) hardcodes `☐` for every objective and suffixes `(<qty> x <item_id>)`; the accepted/active formatter (`formatActiveMissions`, ~4967) instead renders `✓/☐` from `o.Completed` and suffixes `[current/required]` only when `Required>0 && !Completed`. So the same mission legitimately looks different in the two views. Two structs back this: `serverapi.MissionObjective` (board: `Quantity`, no progress fields) vs `serverapi.ActiveMissionObjective` (active: adds `Completed`, `Current`, `Required`, `InCargo`, `InStorage`).
  - **REAL anomaly, and it is SERVER-SIDE, not ours: `percent_complete: 0` while both objectives report `completed: true`.** Both values are decoded verbatim from the wire — `PercentComplete float64 \`json:"percent_complete"\`` (main.go:4872) and `Completed bool \`json:"completed"\`` — so there is no formatter arithmetic to blame. Treat `percent_complete` as unreliable on a freshly-accepted mission and drive completion logic off the per-objective `completed` flags. (Items are PROVIDED here, so both objectives were satisfiable the moment they landed in cargo, which likely explains `completed: true` immediately on accept.)
  - **Correction to an earlier note in this file: there was NO "empty skills reward" bug.** The formatter emits label+value on one line (`"  skills:   %20s %s +%4d xp"`) and cannot print a bare `skills:`; the operator's paste was simply cut off at that line. The XP is reported normally.
  - **Outbound passage: the mission grants a ONE-TIME TRAVEL PASS to the stronghold for the delivery** (operator, 2026-07-25) — that is how you reach Voss Redoubt with no pirate standing. But on COMPLETION the access becomes **PERMANENT** (see below), so the pass is only the bootstrap for the delivery leg itself.
- **Chain 2** (entry = `an_introduction`, which unlocks after chain 1 + smuggling **level 3**; first real leg = **`supply_run`**): ~3 longer smuggling requests. **Free stronghold docking is granted by `an_introduction` itself, NOT by finishing chain 2** (verified on engineer-2 2026-07-25), and it arrives as an UNLOCK with pirate reputation unchanged at baseline 10 — the earlier "rep −30 → +10" framing was wrong in both directions. Chain 2's own payoff is credits/XP plus the path to chain 3. [[reference_pirate_base_registry]]
- **Chain 3** (after chain 2): grants **2 wormhole shortcuts into Crimson Pact space from a pirate stronghold** — the only practical way past the Rampart into Crimson markets (e.g. crimson_war_citadel/Krynn, 26 jumps today). Fleet-wide routing win for haul/mb.
- **Full ladder (CORRECTED 2026-07-25):** chain1 (2 missions) → L2 → repeatable couriers → L3 → **`an_introduction` → FREE STRONGHOLD DOCKING** → chain2 (`supply_run`, …) → chain3 → Crimson wormholes. Chains 2&3 = **Phase 2**. The stronghold-access gate is 1 mission earlier than modeled: **`an_introduction` is the whole onboarding for a stronghold resident.**
- **Phase 2 cohort = stronghold-resident marketbots** (star-named accounts: marketbot_alhena, marketbot_algol, marketbot_bellatrix, sheratan, starfall, xamidimura, zaniah, barnard_44… — created, in NO fleet yaml yet). Purpose: resident market-scanning AT pirate strongholds. Chicken-and-egg RESOLVED: each must run chain 1 → L3 → **`an_introduction`** from safe space first (that one mission grants free stronghold docking; its outbound leg rides a one-time mission pass), then relocate to its assigned stronghold. Chain 2 is NOT required for residency. Smuggling bootstrap = REQUIRED onboarding for these. Stronghold-per-marketbot mapping TBD (4 known: sable_port/crix_stronghold/kael_arsenal/voss_redoubt [[reference_pirate_base_registry]]).
- **Phase 2 beneficiaries (per-agent):** (1) stronghold-resident marketbots — **`an_introduction` REQUIRED** (not chain 2) to dock/scan at stronghold; (2) SHUTTLE fleet — `an_introduction` → haul passengers to pirate strongholds unharmed [[project_passenger_feature]]; (3) haul/mb — chain 3 Crimson wormholes → Crimson markets; (4) **mission runners themselves** — board often has ordinary `delivery` missions to a stronghold (uncompletable without standing); `an_introduction` makes them acceptable = the direct "more missions overall" win.
- **Code note:** buildMissionCandidate checks jumps-reachability but NOT dockability → add a stronghold-destination gate (reject stronghold-dest deliveries unless pirate rep clears it). Same missions pass once chain-2 rep earned.
- **Account model: every agent is its own account (NO shared accounts).** So pirate-rep + Crimson-wormhole unlocks are PER-AGENT — benefit only the agent that ran the chains; no single-agent shortcut for the fleet. Phase 2 = opt-in per agent, targeted at agents that route through strongholds/Crimson. (NOTE: contradicts stale random-6/random-7 shared-account note in [[reference_overmind_launch_commands]] + fleet yaml — operator says no shared accounts.)
- **XP model:** per-level; next_level_xp = threshold within level. CONFIRMED thresholds: L0→1=60, L1→2=165, **L2→3=340, L3→4=585**. The 2 chain-1 missions = +300, and that alone reaches L2 (verified: engineer-2 ended chain 1 at level 2, 115/340); the standalone `a_word_in_private` adds +50 on top. L2→L3 needs +225 more (≈2-4 courier runs) → unlocks chain 2. Cross-border leg 2 (Sol) crossed CLEAN on the freighter via continuous travel — evasion approach validated. Easy missions alone (intro+leg1=150) fall short, and couriers are level-2-locked → **the ONLY path to L2 goes through the cross-border `across_the_line` run.**
- **Couriers** (once L2): type smuggling, provided items, from Cache they're long hauls (altais 14 / sol 17 / nexus_prime 24 jumps), 300-1400cr + 50-175 XP. Too far/short-expiry for current gates (maxJumps=5, minExpiry=180) — must be found NEAR each worker.
- **Ship selection:** FREIGHTERS ARE NOT IDEAL (slow + scan-prone). Concrete selector: `ships.inherent_capabilities` JSON (serverapi InherentCapabilities) has a `scan_resistance` capability w/ numeric value on some hulls — prefer these; higher=safer. Catalog scan-resistant hulls (value): solipsism 50(+cloak60), probable_cause 40(+fuel_eff25), dust_devil 30, interstice 25, terms_and_conditions 25, absence 20, dirk 20, parallax 20, reticence 20, cloister 15, arbitrage 15, attenuation 15, qualia 15, residuum 15. Fits: probable_cause/solipsism=couriers/cross-border; **reticence/cloister = scan_resist + PASSENGER berths = shuttle-to-stronghold.** engineer-2's Bulk Terms freighter = no scan_resist = worst-case pilot.
- **Customs:** continuous travel ≈ zero scan risk (no stop ≥10 ticks at a border); back door Epsilon Eridani→Sol. See [[reference_customs_mechanics]].

**Mission-capture infra ALREADY EXISTS** and matches dedup-catalog+sighting-log model: `mission_templates` (PK id) + `mission_objectives` + `mission_template_locations` (per base,mission sighting). `worker.KBUpdateMissions` / play_as `update_missions` / fleet `kb_update`. **GAP:** entries with empty `template_id` are skipped (capture.go:790 "N procedural skipped") — the parameterized couriers/trade-runs (`..._<route>~<hash>`) are LOST. Fix: synthetic template id = mission_id before `~` + `procedural` column.

**3-part plan** (spec: docs/superpowers/specs/2026-07-25-smuggling-enablement-design.md):
1. Capture procedural missions (synthetic id + procedural flag).
2. Enable category=smuggling in missionrunner: `missionTypeSmuggling` const (mission_select.go:108), generalize `deliverShape` to accept smuggling, relax the Warnings auto-reject for smuggling, add accept-switch case (mission.go:629 + resume ~1141), fleet yaml mission_categories, fix tests (mission_select_test.go:70-98). Parallel to freight fence. [[project_freight_probationary_cargo_fence]]
3. Level-2 bootstrap (flag --smuggling-bootstrap, default off, canary): trigger on skill<2 / skill_required; KB-find nearest chain-entry station; run chain in chain_next order to L2; continuous-travel evasion on the cross-border leg. Mirror [[project_freight_probation_bootstrap]].

**Ops note / STANDING STATE (2026-07-25):** engineer-2 is **intentionally PARKED OUT** of the mission-learn fleet (in `data/overmind/mission-learn-overrides.json` removed[], alongside craftsman-1) as the **standing smuggling pilot** for later Phase-2 mapping — operator chose "stop here, hold engineer-2 out." NOT auto-managed; do NOT re-add without operator say-so. To resume driving: `go run ./cmd/tools/play_as engineer-2` (session is free while parked). To return it to earning: remove from removed[] + SIGHUP overmind. It flies a **Bulk Terms freighter** (no scan_resistance — swap to a scan-resistant hull before more cross-border smuggling). [[reference_overmind_launch_commands]] [[feedback_play_as_go_run]]

**⭐ 2026-07-25 (later) — BOTH chain-2 preconditions are now MET on engineer-2; nothing blocks Phase 2.** It reached smuggling **L3 (417/585)** on courier runs (from 115/340 — couriers are a confirmed repeatable XP source; new threshold data point **L3→4 = 585**), and chain 1 was already COMPLETE because chain 1 is only the 2 Dara Kesh missions, both of which it ran. The earlier "leg 3 `an_introduction` is still unrun, that's the blocker" note was WRONG — `an_introduction` is chain 2's entry, not a chain-1 leg.

**⭐ 2026-07-25 — craftsman-1 has ALREADY BUILT most of the contraband production facilities** (operator). Consequence for Phase 2: contraband inputs (`starshine`, `void_dust`, `nerve_burn`, and the ghost_rounds line) can be **produced in-house rather than bought**, so courier/chain runs that require sourcing are not gated on thin contraband markets. This also makes facility-only recipes the *realistic* costing basis for contraband — which is why `price`'s BoM expander prefers the cheapest facility route (operator decision, same day): e.g. `copper_wiring` at 2 ore/unit via the facility-only `process_copper_wiring` vs 8 ore/unit hand-crafted. craftsman-1 is the production owner and is **parked out of the mission-learn fleet for exactly this reason — do NOT re-add it.** [[reference_bom_table_lossy]] [[project_crafting_brain]]

**⭐🔴 2026-07-26 — PART 2 IS DEPLOYED AND CHAIN 1 RAN ITSELF. Canaries = engineer-1 + explorer-12 only** (`mission-learn-fleet.yaml`, `mission_categories: [delivery, exploration, smuggling]`), live since 18:13Z on worker `v0.2.5-10-gf522cc2`. Checked 23:33Z (5h20m in), both healthy, 0 restarts. The commit message's "chain 1 remains unreachable autonomously" is now WRONG — dropping the flat distance cap (`f522cc2`) is what unlocked it:
- **engineer-1 completed BOTH chain-1 legs unattended**, from `treasure_cache_trading_post`, per `market.db mission_results`:
  - `no_questions_asked` 18:25:52Z→18:30:14Z, 3 jumps, paid **300 cr (advertised 800)**, fuel 168 → net +132.
  - `across_the_line` 22:29:02Z→22:47:34Z, **17 jumps CROSS-BORDER into Sol, clean**, paid **605 cr (advertised 2500)**, fuel **952** → **net −347. The marquee leg LOSES money.**
  - +300 smuggling XP ⇒ **engineer-1 is now smuggling L2** (inferred from thresholds, not verified live — no `get_skills` since; it owns its session so a check needs the freeze dance).
- **REWARDS PAID ARE 24–37% OF BOARD VALUE** (300/800, 605/2500; cf. engineer-2's `an_introduction` 1605/3000 = 53.5%). The mission gate prices off `expected_reward`, so on smuggling it is **structurally over-optimistic and will keep buying losers**. Wants a smuggling-specific haircut before wider rollout. [[project_haul_revenue_halved_v0547]]
- **Gate behaves exactly as designed:** post-launch the canaries logged **zero** `not a plain deliver mission` rejections (163 before opt-in), while non-canaries still reject smuggling fleet-wide (2083 such skips) — the opt-in fence holds.
- **Zero courier accepts, and the blocker is now purely economic**, not the category gate. Canary skips today: **801 expiry** (courier windows ~180 ticks vs 312–504 ticks needed for 11–27-jump runs — the boards near these two only issue long hauls), **26 net-below-floor** (4 genuine near misses at +216/+224/+400/+488, rest −380…−2312), **4 `no reference ask`** on `smuggling_black_*` (contraband has no market ask to price against). **Floor 500 is the binding lever for the near misses.**
- **engineer-1 hit the predicted `skill_required: Courier runs require smuggling level 2`** at 20:11–20:12Z (between the two legs, i.e. at L1) — and **retried the same two couriers 6× in 50s. There is NO accept-failure negative cache**; it stopped only because the missions aged out of the expiry gate. Same class as the freight `wrong_origin` loop — worth a per-mission failure blacklist. [[project_freight_wrong_origin_return]]
- **explorer-12 is an inert canary:** parked at `alpha_centauri_colonial_station`, zero smuggling XP, and chain 1 boards ONLY at `treasure_cache_trading_post` → it can never clear the L2 courier gate where it sits. If a second data point is wanted, route it through treasure_cache.
- **Procedural capture (Part 1) IS live:** `mission_templates.procedural` column exists in `data/spacemolt-knowledge.db` (NOT market.db — market.db only has `mission_results`), 13 smuggling templates captured incl. 5 procedural `smuggling_courier_*`/`smuggling_black_*` rows. Caveat: `SQLITE_BUSY` warnings are still dropping some upserts under fleet load.

**⭐🔴 2026-07-27 — THE REAL LEVER IS THE HULL, NOT THE GATE (operator).** *"these missions should be done in tier 0 or tier 1 ships, not mission cargo ships so the fuel cost is 1cr/jump."* Smuggling couriers carry **3–5 units**; running them in a 355-cargo mission hauler is what makes fuel swamp the reward. Measured on engineer-1's hauler, `fuelCostFor(13)` ≈ **2288 (~176 cr/jump)**, so a 300 cr courier prices at −1988 and is correctly refused. In a tier 0/1 hull the same 13-jump run costs **~13 credits**: net **+287 on credits alone**, +1537 with XP — it passes without needing any of the gate concessions below. **Assign a cheap small hull to the smuggling runner before tuning economics any further.** This compounds the already-noted hull requirement for smuggling — freighters are slow and scan-prone, and the scan-resistant list (probable_cause, solipsism, dust_devil, …) is further down this note. The gate work below stays valid as a margin for marginal runs, but it was compensating for a bad ship choice.

**⭐ 2026-07-27 — PART 3 (the economics gate) BUILT.** engineer-1 was confirmed live at **smuggling level 2, xp 88/340** — eligible for couriers (`skill_required` has never reappeared since chain 1) but accepting NONE, because our own gates rejected all 17 candidates: 10× `net below floor 500`, 6× `expires < 180 needed for 0 jumps`, 1× `no reference ask`. All three price in credits; these runs pay in XP. Three changes in `pkg/worker/mission_select.go`:
- **`missionSmugglingXPCreditValue = 25.0`** — smuggling XP is priced into the gate (credit `Net` is left untouched for reporting; a separate `gateNet` adds `xp × 25`). Rationale: the 252 XP engineer-1 needs for the chain-2 unlock costs ~6300 credit-equivalent, less than one fat haul run, for permanent stronghold docking. Deliberately not generous enough to wave everything through — a 175-XP courier survives a 2000 cr loss, a 5-XP one does not.
- **`missionSmugglingMinExpiryTicks = 30`** (vs 180) — the base margin was travel-blind and refused six black-market jobs boarding AT the worker's own station for runway they never needed. Per-jump allowance unchanged, so anything with real travel prices as before.
- **Hard reject when `buyQty > 0` on smuggling.** Operator, 2026-07-27: there are **two smuggling shapes** — goods provided on accept (*"slides you 5 units of raw gutter flux"*), or *"source the goods yourself / buy on the market"*, which is **UNCOMPLETABLE because contraband has no sell orders anywhere**. The old `no reference ask` rejection was catching these by accident; that is now explicit, and deliberately does NOT lean on the ask lookup failing, since one stale ask would admit a run the worker can never finish.

Verified against a real board trio the operator captured (Grand Exchange → Frontier Station: 3 couriers, each +300 cr / +50 xp, goods provided, 13 jumps at the measured ~56 cr/jump): each is a −428 credit loss, gate-net +822, accepted. Full `pkg/worker` suite green, lint clean.

**Follow-up NOT built — batch fuel sharing.** Smuggling boards commonly offer 2–3 couriers to the SAME destination (the trio above), and `SelectMissionSet` already stacks by `DestSystem` — but `buildMissionCandidate` charges the FULL fuel bill to each candidate independently, so a collectively-profitable batch can be rejected three times before stacking ever sees it. Pricing shared fuel across same-destination smuggling candidates would admit batches that individually miss the floor.

**NOT DEPLOYED:** the mission-learn fleet still runs the 11:12 `f522cc2` binary; this needs a rebuild + relaunch to take effect. At 50 XP/courier engineer-1 needs ~5 runs (fewer batched) to clear 340 and unlock chain 2.

**Next when resumed:** engineer-2 is at **smuggling L4 with free pirate-stronghold docking**; chain 2 is open at **`supply_run`**. Run/capture `supply_run` and the rest of chain 2 → chain 3 (Crimson wormholes). Then turn the spec into a phased implementation plan (writing-plans). Swap engineer-2 off the Bulk Terms freighter to a scan-resistant hull first — it has no `scan_resistance` and chain 2's legs are longer runs.
