---
name: project_current_status
description: "Where we are + next action. As of 2026-07-10: A2 planner SHIPPED + live smoke-tested; sentinel-route fix b6b89c9 on main (1 ahead, push pending). NEXT: Executor B." — resume entry point
metadata:
  node_type: memory
  type: project
  originSessionId: ab1c49b4-60dc-40cf-b763-b03d8d7b4831
---

## 2026-07-10 (later) — A2 LIVE SMOKE TEST DONE + sentinel-route fix committed

**`play_as build` smoke-tested against live data (as craftsman-2 — craftsman-1 is the user's own running play_as session, don't steal it).** All plan shapes verified; `station_core x1` (the motivating case) produced a 124-node distributed plan in 479ms. One real bug found + fixed via TDD, committed **`b6b89c9` on main — PUSHED to origin 2026-07-10 (`bdd4e93..b6b89c9`)**: the `any_docked_station` sentinel never resolved in source_sql → every haul UNKNOWN_ROUTE + wrong-base blame. Fix: exported `craftbrain.DefaultCraftBase`, SystemOf maps it to origin system, inventory.go blames destination when destSys="". Details in [[project_crafting_brain]].
- Pre-existing reds on main, NOT branch-caused: `pkg/game TestServerCommandsCoveredByClient` now fails on new server command **"espionage"** (v0.485.0 drift, see [[project_server_restart_warning_event]]); pkg/worker roles.yaml red from earlier.
- **NEXT: Executor B spec** (contract facts in [[project_crafting_brain]]), then deferred Task 12 [[project_worker_storage_capture_gap]] — STALE tags on every live haul show why the storage sweep matters.

## 2026-07-10 — CRAFTING BRAIN A2 (PLANNER) SHIPPED: merged + pushed

**State: `origin/main` = `bdd4e93`. Tree clean. Unpushed-main backlog CLEARED (27 commits pushed, `3f71227..bdd4e93`).**

Crafting-brain sub-project **A2 — The Planner** built via subagent-driven development (11 of 12 tasks; Task 12 deferred by the user) and fast-forward merged to main. 21 commits, 26 files, +3067/-9.

- Ships `pkg/craftbrain`: `graph.go` → `inventory.go` → `site.go` → `expand.go` → `format.go`, behind a `Source` interface (in-memory fake + SQL impl at `cmd/tools/play_as/source_sql.go`).
- Operator command: **`play_as build <target> [qty] [--json] [--max-hand-ticks=N]`** — qty is **output units**, not runs. Read-only; **dispatches nothing; no fleet redeploy needed**. Run via `go run ./cmd/tools/play_as`.
- The push also carried the 6 previously-unpushed commits, incl. `8df24f8` (3-section facility capture) and `21e60dc` (`last_seen_utc`) that A2 depends on.
- Live DBs were never written during the build; `public_facilities` grew 848 → 855 purely from the fleet's own capture.

**NEXT ACTIONS, in order:**
1. **Smoke-test `build` against live data** — never run outside fixtures. `go run ./cmd/tools/play_as` → `build <recipe_id> <qty>`. Expect BLOCKED to be common (~68% of `facility_only` recipes have no known facility); the coverage footer says "not swept yet" vs "impossible".
2. **Executor B** — its contract MUST state that `Node.DependsOn` may be cyclic (cycle reported in `Diagnostics`; A2 never loops, but a DAG-assuming consumer will). Also handle `StatusUnknownRoute` (Jumps omitted, not 0) and treat a BLOCKED-node plan as a successful run of an incomplete plan.
3. **Deferred Task 12** — hourly `view_storage` sweep. Not a roles.yaml one-liner; see `project_worker_storage_capture_gap.md`. Needs its own spec + a fleet redeploy.
4. Smaller A2 follow-ups (render `Plan.Leaves`, `Buyable` diagnostics note, mineability lookup) — see `project_crafting_brain.md`.

Also open, unrelated: `server_restart_warning` is an unhandled response type and `BuiltForAPIVersion` (0.473.0) is behind the server (0.485.0) — see `project_server_restart_warning_event.md`.


Resume entry point — leads with the freshest. Update or delete as work moves; not a permanent record.

## ⭐ SESSION 2026-07-09 (FACILITY CATALOG A1 — SHIPPED to main (local) + mb fleet REDEPLOYED)
**Crafting-brain sub-project A1 (facility catalog) built via SDD + MERGED to main (local FF `caa0478..7f0b0eb`, 8 commits; main now 10 ahead of origin — NOT PUSHED).** Full detail in [[project_crafting_brain]]. Galaxy-wide store of public production facilities (recipe→stations/tier/fee): `public_facilities` table (migration **48**), `pkg/knowledge/public_facilities.go` (`UpsertPublicFacilities`/`FacilitiesForRecipe`, concrete *SQLiteKB not on Base iface), capture in `pkg/worker/capture.go` (`upsertPublicFromFacilityList`, best-effort before base-gate), play_as **`where_facility <recipe_id>`**, hourly `facilities` sweep on resident* marketbot roles. Cost col = `rental_fee_per_run` (server `production.rental_fee_per_run`; details_json holds per-unit price + throughput). Field mapping from openapi (NOT a live sample — details_json is the safety net).
- **DEPLOYED to mb fleet:** rebuilt bin/worker+bin/overmind; graceful-restarted mb overmind (old pid 3424009 → new **498518**) via drain(USR1)+TERM+rm mb.sock+relaunch (exact cmdline `--fleet data/overmind/mb-fleet.yaml --socket data/overmind/mb.sock --worker-bin bin/worker --status-file …mb-status.json --history-file …mb-history.jsonl --market-db-path data/market.db --stagger 10s`). Confirmed: `facilities` registered into each marketbot schedule.json; scheduler **retries on failure + backfills on startup**. **✅ DATA IS LANDING — FULLY VALIDATED LIVE:** by 20:00Z first rows, growing fast to **247 rows / 6 stations / 149 recipes / 8 owner factions** within minutes; `where_facility grow_quantum_dot_matrix` returns 2 competing sites (unknown_edge_waystation T2 vs confederacy_central_command T1) — openapi field mapping CORRECT on real payloads (recipe/level/fee/owner all sensible). Monitor: `sqlite3 data/spacemolt-knowledge.db "SELECT COUNT(*),COUNT(DISTINCT station_id) FROM public_facilities;"`. **BUG FOUND+FIXED (`2af5198`):** renaming labor_cost→rental_fee_per_run by editing migration 48 IN PLACE was wrong — the live db had ALREADY applied original-48 (user's earlier play_as created the table w/ labor_cost), and an in-place edit never re-runs on an already-migrated db → capture failed `no column named rental_fee_per_run`. Fix = idempotent post-migration fixup `ensurePublicFacilitiesRentalCol` (renames if old col present; no-op on fresh) + healed the live db directly (opened via new-code KB) + rebuilt bins. **LESSON: never edit an applied migration in place; add a new migration / self-heal fixup.** Fix commit `2af5198` sits on main after the A1 8-commit range (9 feature commits total). **PUSHED to origin/main 2026-07-09 (`5e46c53..2af5198`, 11 commits incl. earlier plan/spec).** bin/worker+overmind rebuilt with the fixup (respawns self-heal); running mb fleet (new pid 498518) already capturing on the healed live db — no further restart needed.
- **Also shipped (out-of-band):** `fix(play_as)` **`set_output_price`** (`caa0478`) — positional arg mapped to `item_id` not `price`; now `["price"]` only + fractional-price parse (server allows 0.25). Live in working tree.
- **NEXT (crafting brain):** A2 Planner spec (target→BOM→subtract faction-storage→site via this catalog→JSON step-DAG), then B Executor. Also PUSH pending (main 10 ahead of origin). Queued: [[project_overmind_stall_kill_connect_loop]].

## ⭐ SESSION 2026-07-07 (ASSIST dock-in-place fix — SHIPPED to main + DEPLOYED to assist fleet)
**`assistEnsureHome` dock/undock thrash FIXED (`ea37877`, committed to main, not yet pushed).** Bug: it always ran Autopilot to the home POI even when already sitting there; Autopilot's travel auto-undocks, so re-navigating to the current POI undocked the ship every standing pass → perpetual undock↔dock, refuel never landed. Hit **assist-frontier** parked at Outerrim's `mobile_capital` (moving POI, jumps daily): pinned undocked at 1/120 fuel, would be left behind on the daily jump. Fix: only travel when `st.CurrentPOI != HomeStation`; else dock (+refuel) in place. Added `TestAssistEnsureHomeAtMobileCapitalDocksInPlace`. Helps every assist worker; decisive for mobile home.
- **Deployed:** rebuilt bin/worker+bin/overmind, TERM'd assist overmind (pid was 3881643) → relaunched (new pid 4171220) with exact cmdline (`--socket data/overmind/assist.sock --fleet data/overmind/assist-fleet.yaml --worker-bin bin/worker --status-file …assist-status.json --history-file …assist-history.jsonl --stagger 10s`). Verified: frontier now **docked at mobile_capital, refueled 1→99/120**, rode the jump to The Telescope; ZERO undocks since restart. All 5 assist workers docked+healthy. Residual log line "You are already at the target system. Then travel to Mobile Capital" = per-pass `find_route` home-resolver echo (harmless no-tick query), NOT travel.
- **Note:** frontier now has ~1 credit (spent its 519 refueling); assist economics rely on rescue-fee reimbursement (1000cr/rescue) to replenish. **PUSH still pending** (this commit + the 12 prior from 07-06).

## ⭐ SESSION 2026-07-06 (PRICE COMMAND — SHIPPED to main; SDD build)
**`price <item_id>` play_as REPL command MERGED to main + PUSHED to origin (`baeaf1a`; `77a1fe0..baeaf1a`).** Suggests a per-unit `create_sell_order` price = component/ore market cost + separate 20% margin line; flags underpriced items (market ask ≫ build cost). Motivated by kb "did_you_know" build-cost analysis (~87% of items unbuildable from market today; many finished goods priced ≫ parts). Built via subagent-driven-development (8 commits, 7 tasks + 1 fix wave, all reviewed clean; opus final review READY-TO-MERGE).
- **Shape:** two cost bases (Nearby = local + ≤2 hops; Market-wide mean ask) × two decompositions (one-level recipe inputs; fully-decomposed BoM ore). Flags: `--margin=20 --hops=2 --mode=both|recipe|bom --json`. Output: per-component table, build-cost/per-unit/+margin/SUGGESTED rows, CURRENT MARKET (nearby+mkt ask, best bid), UNDER/OVER/FAIR verdict, feasibility/shortfall line.
- **Code:** new pure `pkg/pricing` (rollUp/askStats/classify/Report — reuses `finditem.Find` for ask-side, no nav code) + `cmd/tools/play_as/price.go` glue (resolveRecipesForOutput/recipeComponents/pickBestRecipe/renderPriceText/handlePrice). Dispatch `case "price"` in main.go.
- **Key facts baked in:** recipe.ID ≠ output item_id (resolve by scanning Outputs); recipe mode ÷ output-per-run, BoM mode ÷1 (bill_of_materials qty already per-unit); single-best-ask pricing NOT depth-walk ([[project_price_command_depthwalk]]); items-only MVP (ships/raw-ore → "not craftable"/"no BoM"). Final-review fixes: multi-output divisor selects matching output; pickBestRecipe guards incomplete bases (+Inf, altMkt only when Complete); --mode validated.
- **NOT deployed** (play_as = on-demand `go run`, no fleet restart). **LIVE REPL SMOKE deferred to operator** (needs live creds + market.db/crafting.db). Spec: `docs/superpowers/specs/2026-07-06-price-command-design.md`; plan: `docs/superpowers/plans/2026-07-06-price-command.md`.

## ⭐⭐⭐⭐⭐⭐⭐⭐⭐⭐⭐⭐⭐ SESSION 2026-07-06 (RESCUE-FEE REIMBURSEMENT — SHIPPED to main + DEPLOYED to haul+assist; plan-command units-vs-runs bug fixed)
**Two workstreams, both DONE + committed on main (NOT pushed — 12 commits ahead of origin/main; offer push).**

**(A) plan-command units-vs-runs bug (`9c3d61c`, craftplan) — systematic-debugging + TDD.** Root cause = last week's server craft restructure made recipes yield N>1 output UNITS per run (barrel_assembly 2/run, h2_fuel_combustor 100/run), but `plan` still multiplied the CLI number as if it were RUNS → `plan barrel_assembly 100` reported materials for 100 runs (2× real cost; queue only needed 50 runs for 100 units). Fix = interpret CLI qty as output units, `runsFor(r,units)=ceil(units/r.Outputs[0].Quantity)`, thread `runs` through material math in BOTH `pkg/craftplan/direct.go` (planDirect) and `reachable.go` (planReachable + collectIntermediates); new `PlanResult.Runs`; header now `xN units → M run(s) (…, K/run)`. Craft-command suggestion still passes UNITS (server craft takes units, per user's "Queued 100 Barrel Assembly (50 run(s))"). **play_as tool change — NO deploy needed** (live on next `go run ./cmd/tools/play_as`). Mutation-checked goldens.

**(B) Rescue-fee reimbursement — full SDD (3 tasks + hardening), opus final review = READY-TO-MERGE YES.** Spec/plan: `docs/superpowers/{specs,plans}/2026-07-05-rescue-fee-reimbursement*`. Rescued haulers gift their assister a flat fee (`send_gift shipside_assist_<home> credits 1000`) on next dock so assisters stop going broke re-tanking (they held 3–1700cr while haulers hold 0.5–2.5M).
- **Loop:** overmind `pollRescues` on a `done` record with gate `fee>0 && ClaimedBy!="" && RescueFuel>0` (excludes tows/operator-flips/skip-release) → `rescue.AppendDebt` writes `data/agents/<strandee>/rescue-debts.json` → relaunched worker's `RunStanding` idle pass calls `worker.PayRescueDebt` → pays head debt via send_gift when docked, pops on success. Cross-process file access PROVABLY safe (strandee process killed BEFORE quarantine flag → producer/consumer never concurrent).
- **New:** `pkg/rescue/debt.go` (Debt{Recipient,Credits}, AppendDebt/LoadDebts/RemoveHead); `cmd/overmind` `--rescue-fee` flag (default 1000); `pkg/worker/rescue_fee.go` (GiftClient iface, PayRescueDebt); StandingDeps.PayDebts hook.
- **2 hardening fixes (user chose both, `1174b94`):** (1) SOLVENCY GATE — PayRescueDebt skips when `st.Credits < debt` (kills broke-assister infinite-retry loop; haulers unaffected). (2) DEBT-AFTER-ARCHIVE — `archiveRescue` returns bool; fee-debt + ReleaseQuarantine gated on successful archive so a queue-write failure retries clean and debt lands exactly-once (mutation-verified test, root-guarded chmod injection).
- **Commits:** daafb51,7ecdb68 (T1), 1c7deb6 (T2), b1caedf (T3), 1174b94 (hardening). Accepted-as-shipped: RemoveHead-after-gift double-pay (spec'd best-effort); release-to-pending attempt-tracking (separate).
- **DEPLOYED ~01:07 local:** rebuilt bin/overmind+bin/worker (00:53). Assist drained (SIGUSR1→"fleet drained 5/5")→TERM(clean)→relaunch (new PID). Haul drained to 19/21 (trader-2/trader-7 perpetually mid-travel, never idled — TERM'd them mid-travel, safely resumable)→relaunch **+`--stagger 10s` added** (was launched without it; 21 fresh logins). **Live: haul 21/21, assist 5/5, both overminds healthy, no errors.** Rescue-fee loop live end-to-end; fires on next haul strand.

## ⭐⭐⭐⭐⭐⭐⭐⭐⭐⭐⭐⭐ SESSION 2026-07-05 (DYNAMIC RESCUE-FUEL SIZING — SHIPPED to main + DEPLOYED to assist fleet)
**Overnight check found the fuel-death loop was NOT converging:** flat rescue_fuel≈10 (enqueue-side `fuelForHops`=5×hops+5 floor 10; most strandees in a station-system → hops 0 → 10) is below one big-hauler jump, so 420/240-tank haulers burn it instantly and re-strand. 30 rescues since 02:16Z, salvager-10 ×11 / trader-1 ×8 / trader-8 ×2, still looping at 16:56Z. `refuel target=<player>` is a **ship-to-ship transfer from the RESCUER's own tank** (needs Refueling Pump) → naively raising it drains the rescuer (assist-frontier was at fuel 1/120).
**Built via full SDD (brainstorm→spec→plan→2 tasks→task reviews→opus whole-branch = READY-TO-MERGE YES, 0 Critical/Important).** Spec/plan: `docs/superpowers/{specs,plans}/2026-07-05-rescue-fuel-sizing*`.
- **What shipped:** (1) pure `rescue.TransferQuantity(strandeeMax, strandeeFuel, rescuerFuel, hopsHome) = min(need, spare)`, need=max(0,max−fuel), spare=max(0,rescuerFuel−(5×hopsHome+5)) — in `pkg/rescue/enrich.go`. (2) `pkg/worker/assist.go` `runRescue` now sizes the transfer live via new `rescuerHome`+`rescueFuelQty` (rescuer's GetState fuel + BFS hops to its own home). Falls back to `rec.RescueFuel` if state/KB/route missing (keeps old no-KB test green). If spare≤0 → skip refuel + release claim to pending (ClaimedBy cleared) so a fuller/nearer rescuer takes it — "assist always keeps enough to get home" is a hard invariant (user's design). Done-transition records the ACTUAL qty into RescueFuel (rescue-history now shows real amounts, not flat 10). enqueue-side `FuelForSystem` left untouched (= the fallback).
- **Merged fast-forward to `main` @ `69804fb`** (branch fix/shuttle-targeting kept, points at main). **NOT pushed** (local deploy only — offer push next).
- **DEPLOYED ~16:24 local:** rebuilt bin/overmind+bin/worker (**fix lives in bin/worker** = assist runRescue). Assist overmind drained (SIGUSR1 → "fleet drained 5/5 idle") → TERM (clean, no orphans) → relaunched detached (setsid, same invocation, --stagger 10s, new PID 3758780). All 5 assist up+healthy; **assist-frontier recovered fuel 1/120 → 120/120**. New sizing fires on the NEXT haul strand (salvager-10/trader-1/trader-8 still looping on OLD haul binary until they re-strand → next rescue uses the new logic).
- **Deferred Minors (from opus final review, in SDD ledger):** M1 `qty≤0` conflates can't-spare vs strandee-full(need==0) → release loop, but unreachable now (fuel-quarantined worker never full-tank); M2 release-to-pending has no backoff → chronic small-tank/distant rescuer ping-pongs one record each pass (spec'd; add attempt-counter/not-before ts later); M4 enrich clamps could use max()/min() builtins. **Refueler HARDWARE roadmap saved separately: [[project_refueler_ship_roadmap]]** (expanded_fuel_tank +100 utility module early; Tanker Tier2/Tier4 hulls 1500–10000 fuel long-term — bigger rescuer tanks = bigger transfers).

## ⭐⭐⭐⭐⭐⭐⭐⭐⭐⭐⭐ SESSION 2026-07-04 PM/EOD (QUARANTINE+RESCUE DEPLOYED + VALIDATED + 2 LIVE FIXES; SHIPS TAB; find_item) — main @ `eeb144a` pushed, ALL CLEAN
**Deploy done (~19:00 local):** rebuilt bins; drained (20/21; salvager-8 never idled — force-stopped per overmind's own advice) + restarted haul overmind (PID 3527864); launched assist overmind. **Live results:** 11 quarantines, 6 auto-rescues (+10-fuel→dock→full-tank loop works), auto-rejoin works, live `refuel --target` VALIDATED (operator manual rescue of salvager-3: +116 fuel via assist-frontier `refuel --target="Barrel 'Bin' Baxter"`).
**Live fix 1 — ghost-home election (`0dea9b3`):** assistElect deferred to nearest home incl. absent agents' homes — rig-less assist-krynn (benched at launch) was nearest to 3 strandees → nobody claimed 40+ min. Fix = rank-based takeover (assistTakeoverInterval=5m per rank; unparsable RequestedAt = infinitely old). Validated: all 3 claimed in parallel within minutes.
**Live fix 2 — mid-jump resume (`eeb144a`):** worker restarted in hyperspace gets find_route "You are not in a system" → runRescue marked record FAILED (assist-haven/salvager-10 incident during the krynn re-add restart). Fix = in-transit navigate error keeps the claim, retries next pass. Validated live same restart (trader-8+salvager-10 logged "retrying next pass").
**FINAL EOD STATE (21:35): ALL RESOLVED.** Rescue queue EMPTY; haul 21/21 seen+healthy; assist 5/5 (krynn refitted+back). Every one of the 11 quarantined workers recovered. The last 3 (trader-1/trader-8/salvager-10) failed refuel with `different_location` — server had **TOWED the derelicts to the_rampart_checkpoint (a STATION in the_rampart)** from 3 different systems (NEW GAME MECHANIC: empire checkpoint towing of derelicts). Being a station, they didn't need fuel delivery: done-flipped → rejoined → docked → self-refueled to near-full on own credits (381/420, 181/240, 420/420). Also learned: assist rigs deliver fuel WITHOUT credits (assist-nexus did a 14-jump rescue on 15cr).
**Rescue-hardening backlog (bundle these next):** (1) `different_location` refuel failure should RE-TARGET, not fail — the server error names the strandee's actual location ("they are at The Rampart Checkpoint"); parse + re-navigate, or requeue pending with corrected POI. Related: treat record POI as a HINT (stale heartbeat + server towing both falsify it); (2) rescue_fuel=10 undersized — salvager-3 ping-ponged 3× before manual +116; size from real nearest-station hops at enqueue + make rescued hauler refuel FIRST; (3) trader-9 FALSE quarantine — "stalled undocked" while actually docked (docked-flag staleness the 06-29 fix missed?); (4) salvager-8 never-idles in drain (frozen-pass; "schedule last_run advancing" progress signal); (5) cosmetic rejoin log "+0 fuel by " on operator done-flips. ALSO: consider "is strandee at a station POI?" check at enqueue — towed/docked strandees can self-refuel, skip the assist trip entirely.
**Then:** resume feat/haul-ship-replacement tasks 7-9 (rebase onto main; now has 117 hull classes of live ship_listings data); isStorageShape browse_ships false-match (34×/hr empty storage callbacks); market.db display-name system_id capturer root cause; API currentness (client v0.397.0 vs server v0.471.3).
**Also shipped today:** market-dashboard Ships tab (04964d7 + qty grouping e6bd94b + sortable cols 77a9de1; running :8090, --kb-path read-only); catalog refresh to v0.471.3 (ships 332/items 650/recipes 666/skills 30 — [[reference_catalog_refresh_runbook]]); find_item (e1c73ff — [[project_find_item_command]]); view_faction_storage --station_id (d46f022).

## ⭐⭐⭐⭐⭐⭐⭐⭐⭐⭐ SESSION 2026-07-04 (SHIP-LISTINGS CAPTURE REVIVAL + WORKER MISSION CAPTURE — MERGED TO MAIN @ 8863a04 + PUSHED, NOT DEPLOYED)
**browse_ships capture was DEAD since 2026-02-18** (server shape drift {ships:[...]}→{base_id,count,listings:[...]}; GetRawJSON("ships") nil → SILENT skip). Built via SDD (6 tasks + fable final review, READY-TO-MERGE YES, 0 Critical/Important). Spec/plan: docs/superpowers/{specs,plans}/2026-07-04-ship-listings-capture-revival*. What shipped: (1) storeRawJSON classifies browse_ships (base_id+listings+count, NO action) under dedicated raw key `ship_listings` — action guard keeps facility browse_for_sale on "listings"; (2) knowledge.ShipListing v2 wire shape (listing_id/ship_id/class_id/price/tier/hull[-1=unreported]/seller/listed_at; class stats via ship_classes join) + migration 47 drop+recreate + `ShipListingsFromBrowseJSON`; (3) StoreShipListings = REPLACE-per-station (no history; ~5k rows steady, was heading to 120k/day); (4) missing-raw-key now WARNS (never silent); CapturedAt actually set (was year-0001); (5) auto-explorer shares decode path; (6) **KBUpdateMissions ported from play_as into worker kb_update** (docked branch, hourly, all 34 marketbots) — play_as delegates, formatMissionDiffValue deleted. Ship-ok follow-ups in ledger: base_id cross-check on decode; UpsertMissionTemplate BUSY_SNAPSHOT skip; PRE-EXISTING isStorageShape matches browse_ships → spurious empty storage callbacks 34×/hr (fix in isStorageShape). **DEPLOYED to mb fleet 2026-07-04 ~20:50Z** (new overmind PID 3424009, 34/34 up, migration 47 applied live; first 21:00Z boundary: ship_listings = 29/29 stations / 316 rows / 117 distinct hull classes, 34× "Saved ship listings" + 34× "update_missions:" in log, 37 mission templates re-seen). mb fleet is also on quarantine-capable binaries now. **Haul overmind restart (7 strands quarantine) + assist overmind launch STILL PENDING.** Marketbot_krynn zombie fixed same day (frozen scheduler since 07-03 restart, heartbeating-healthy-docked = watchdog blind spot; killed+respawned, capturing again). QUEUED: [[project_find_item_command]] (find_item: market.db item search ranked by jump hops, shared core + play_as cmd).

## ⭐⭐⭐⭐⭐⭐⭐⭐⭐ SESSION 2026-07-03/04 (STRANDED-QUARANTINE + ASSIST RESCUE — BUILT, MERGED, NOT DEPLOYED)
**Full quarantine+rescue pipeline BUILT via SDD (10 tasks + fable final review + fix round), merged fast-forward to `main` @ `690982e` and PUSHED (2026-07-04; includes the assist-frontier mobile_capital fix; main fast-forwarded via `git fetch . branch:main` because live runtime files block checkout). NOT yet deployed to the running fleets.** Spec `docs/superpowers/specs/2026-07-03-stranded-worker-quarantine-design.md` (incl. Deployment runbook); plan + SDD ledger in `.superpowers/sdd/progress.md`.
- **What it does:** supervisor QUARANTINES stranded workers (fuel-dead signature: stalled+undocked+fuel<max(10%tank,10); OR 3 futile stall-restarts ≈60min) — kill once, flag (`quarantined` in status JSON), never respawn; files enriched rescue record (in-game username from credentials.json, KB system_id, rescue_fuel=5×jumps+5 floor 10 fallback 25) into flock-guarded `data/overmind/rescue-queue.json` (sidecar .lock; new `pkg/rescue`); rejoin poll auto-relaunches on record `done`, archives to rescue-history.jsonl. Also FIXED the watchdog double-fire (MarkRestart zeroes LastProgress). New overmind flags: `--rescue-queue --rescue-history --fleet-name --kb-path`.
- **Assist role:** 5 agents assist-{haven,sol,krynn,frontier,nexus} (roster `data/overmind/assist-fleet.yaml`; 4 stations KB-verified; **assist-frontier's home = `mobile_capital`**, Outerrim's daily-hyperspace-jumping capital, resolved live per pass via `find_route mobile_capital` — `assistMobileHomes`/`resolveAssistHomes` in pkg/worker/assist.go, `690982e`, user-directed) run new `assist` standing behavior — deterministic nearest-capital claim election → autopilot → `RefuelShip(username, qty)` (NEW GameClient method, `refuel` w/ target+quantity payload; also on MCPGameClient) → done → home+retank. Ordering invariant: rescue record lands BEFORE quarantine flag (rejoin poll treats flag-without-record as operator-resolved→release).
- **DEPLOY NEXT (runbook in spec):** rebuild bin/overmind+bin/worker, drain(SIGUSR1)+restart haul overmind first → 7 strands quarantine in ≤1 stall window; launch assist overmind (assist.sock); **operator TODO: verify refuel_rig fitted on 5 assist ships + Frontier dock live-check + live `refuel --target` behavior (never tested).** Manual rescue = refuel by hand + flock-wrapped jq status→done (see runbook).
- Accepted follow-ups (triaged, ship-ok): silent hold on fleet-name mismatch; DefaultSpawn doesn't forward --rescue-queue to workers (operator override splits paths); ghost WorkerInfo from off-roster records; archive remove-before-append; empty-target pre-flight check.

## ⭐⭐⭐⭐⭐⭐⭐⭐ SESSION 2026-07-03 (RESTART-HARDENING VALIDATED IN PROD)
**Daily server restart hit 2026-07-03 09:02 local (+ smaller StatusGoingAway bounce 10:41). The 07-02 fixes WORKED: zero new strands.** All 56 workers reconnected in ~30s (mb 34/34 "Reconnected successfully", haul 21/21, shuttle 1/1); reconnect resync (fix C) logs only on failure → silence = clean; marketbots all docked restarts=0; johnny_cab mobile+earning.
- **REMAINING PROBLEM = 7 fuel-stranded haulers + stall-watchdog THRASH.** salvager-3/9/10 + trader-1/5/8/9 are at 0–12 fuel, undocked at NON-STATION POIs (stars/ice fields/comet graves) — the known chicken-egg (can't dock, can't travel; [[project_haul_lawless_routing]]). They were stranded BEFORE the restart: stall loop began 22:48 on 07-02, 15 min after the haul relaunch. The watchdog (fix B) restarts them every 15 min forever (restart can't refuel; counts 105/103/94/70/67/53/52; ~490 stall events on 07-03 alone, still marked healthy=true).
  - **Watchdog gaps to fix:** (1) no backoff/give-up → futile 15-min thrash; (2) **double-fire bug** — killed=true workers log "restart #1" then "#2" in the same second (watchdog fires twice before LastProgress resets); (3) thrashing workers still report healthy=true in status.
  - **Recovery for the 7 = manual tow/gift or a fuel-guard** (refuse to leave a station without round-trip fuel / carry fuel_cells) — same fix family as the 07-01 episode.
- Old stragglers salvager-5/6/8 CLEARED (restarts=0, moved, working) — the 07-02 MINOR TODO verify is done.
- NEXT: (1) decide watchdog backoff + fuel-strand handling; (2) rescue the 7; (3) Frontier public-access KB gap; (4) salvager role.

## ⭐⭐⭐⭐⭐⭐⭐ SESSION 2026-07-02 (SHUTTLE-RECOVERY HARDENING — DEPLOYED FLEET-WIDE)
Branch `fix/shuttle-targeting` (pushed through `6d5283c`). Trigger: johnny_cab (shuttle) frozen 2.5 days at 1.37M in station-less Maplevale; server patches+restarts **≥1×/day**, dropping workers mid-run → stranded undocked → deadlock. Audit (Explore agent) found the shuttle was the only HARD-TRAP role (haulers/explorer already safe; residents/miners home-pinned); two systemic gaps = no supervisor stall-watchdog + reconnect doesn't resync location.
- **SHIPPED 3 staged fixes + a prereq, all TDD/lint-clean, pushed:**
  - **A — shuttle escape hatch** (`5c32f98` + refinement `6d5283c`): `Shuttle()` now gates on the **docked flag** (`!state.Doc`), not `CurrentPOI==""`, and on can't-dock-here calls `shuttleRecoverIfStranded` → `galaxy.FindNearestByPOIType(...,"station")` (same query haulers/databot use) → autopilot to the nearest station POI. Handles BOTH variants: station-less system (Maplevale, "No base") AND non-station POI in a station-having system (Frontier `drifters_haze` gas cloud, loops on ListStationPassengers "dock at a station"). Also intra-system reposition (travel to the station POI in the CURRENT system); no-ops only when already AT the target station POI (`shuttleAlreadyAtStation`, pending dock).
  - **B — supervisor stall watchdog** (`2bfce1b`): `WorkerInfo.LastProgress` (advanced when system/POI/credits/docked change) + `Stalled()` + `StallTimeout=90*SleepTick=15min`. Restarts an **undocked+frozen** worker (docked/drained exempt → residents & hub-camping shuttles never flagged). Fleet-wide net for ANY stuck-but-heartbeating worker (heartbeat-alive ≠ progress-alive; johnny_cab read Healthy 2.5 days).
  - **C — reconnect location resync** (`93f1313`): `Reconnect()` (only ever the already-running path, via EnsureConnected) now pulls best-effort `get_system`+`get_status` post-login so a worker dropped mid-run by the daily restart acts on REAL state (Traveling/CurrentPOI), not stale — realizes the user's "login/welcome hook = restore-needed" idea.
  - **prereq** (`5fcea82`): fixed a pre-existing red test — `TestLoadRolesParsesResident` asserted `idle_mine`, but `570d148` switched resident→`idle_market`.
- **DEPLOYED FLEET-WIDE 2026-07-02 ~22:33** onto freshly rebuilt `bin/overmind`+`bin/worker` (HEAD 6d5283c). All 3 overminds + 56 workers on the new binaries (**0 on old**):
  - **shuttle** overmind PID **3113526**; johnny_cab respawned + earning (self-rescued live from drifters_haze: Dock→No base→Route→Jump→docked at deep_range_outpost; credits 1.37M→1.51M).
  - **mb** overmind PID **3109999**, 34/34 workers docked/idle (stopped clean — SIGTERM overmind sends abort + cancels ctx → workers exit; no drain, per user "marketbots all station-idle").
  - **haul** overmind PID **3117759**, 21/21. DRAINED to 18/21 (SIGUSR1); 3 stragglers salvager-5/6/8 stuck mid-run (salvager-5 = the known station-less dead-end) — force-stopped (drain log advises it), their claimed hauls resume on restart.
- **GOTCHA (again):** a `/proc` cmdline scan for `bin/worker`/`mb.sock` **self-matches your own Bash tool process** (its cmdline contains the pattern) → phantom "won't-die" workers with incrementing PIDs. Filter on `/proc/PID/comm` == `worker`/`overmind` instead. Relaunch = `setsid nohup ./bin/overmind --socket data/overmind/<f>.sock --fleet data/overmind/<f>-fleet.yaml [--status-file <f>-status.json --history-file <f>-history.jsonl] >> data/overmind/<f>-overmind.log 2>&1 &` (haul omits status/history → defaults fleet-status.json/fleet-history.jsonl). Overminds are `setsid`-detached under `systemd --user` (NOT services) → no respawn on kill.
- **MINOR TODO:** Frontier's stations (`expedition_launch`/`scout_docks`) lack public-access base records in KB → `FindNearestByPOIType` skips them, so recovery relocates cross-system instead of to Frontier's own station (still rescues). Fix = ensure station base rows carry PublicAccess. Also the haul stragglers salvager-5/6/8 may still be stuck post-restart — verify hauler recovery clears them.

## ⭐⭐⭐⭐⭐⭐ SESSION 2026-06-29/30 (MARKETBOT IDLE-MINE + GAS/ICE HARVEST — START HERE)
Branch `fix/shuttle-targeting`. **mb fleet is DOWN** (SIGTERM'd overmind 2541492; user manually refitting 7 bots' modules). Redeploy pending refit + procyon confirmation.
- **Idle-mine was ALREADY live for all 34 marketbots** (role `resident`, `idle: idle_mine`, + per-agent schedule.json `quarter_hourly`/15-min `update_market`). BUT the deployed `idle_mine.smolt` had NO `refuel` → undock/travel cycles drained **ALL 34 to fuel=0** (stranded dockside, market-only). FIX committed `ed8f8ed`: **refuel-FIRST** in idle_mine (recovers empty bots + tops up each cycle; trailing refuel can't recover — an empty bot can't undock). Bumped inner `loop -f 20→25 mine`.
- **GAS/ICE extension committed `9a36373`:** 7 marketbots sit in beltless systems that HAVE a gas_cloud/ice_field. New `idle_gas`/`idle_ice` scripts (`$GAS_CLOUD$`/`$ICE_FIELD$` tokens resolve; `mine` is the universal verb, server checks the fitted module) + `resident_gas`/`resident_ice` roles (same market schedule). Reassigned in mb-fleet.yaml — **GAS(5):** cargo_lanes, factory_belt, procyon(DEFAULT-gas, flip to ice if user fits ice), sirius, the_crucible; **ICE(2):** alpha_centauri, the_levy. **Requires the user to manually swap mining_laser→gas_harvester/ice_harvester** (in progress). **4 truly barren** (no asteroid/gas/ice POI): traders_rest, the_rampart, synchrony, blood_forge → stay market-only. `.gitignore` negations added for the 2 new scripts.
- **DECISION — captures-first (user 2026-06-29):** mining UNDOCKS bots, which competes with their docked-only captures (`update_market`/`kb_update` no-op when not at a station). User chose: **only mine when it won't overlap a capture boundary.** PENDING BUILD = a boundary-aware idle gate in RunStanding (mine only if time-to-next-boundary > est cycle duration). FIRST measure cycle duration (~10 min est for 25 mine) + how many complete in the ~10-min window; then shorten the mine count + build the gate. NOT STARTED (measure after redeploy).
- **`ship_listings` capture DEAD since 2026-02-18** — `kb_update`→`KBUpdateStation`→browse_ships→StoreShipListings IS wired but **gated on being docked**; bots off mining at the hourly boundary → skipped. Revive by capturing while docked (ties into captures-first). Passenger-demand capture still NOT built ([[project_passenger_demand_intel]]). The `passengers` KB table (1736 rows) is only an identity log (name/bio/sightings), NOT demand (no origin→dest→fare).
- **MINING-POOL IDEA (user 2026-06-29, FUTURE):** turn remaining/idle agents into a dedicated `mine.sock` overmind pool reusing the idle_mine/gas/ice roles; **`miner-*` agents are the confirmed seed.** Needs: mine-fleet.yaml roster + per-agent home-belt/module + belt DISTRIBUTION (avoid depleting one belt) + maybe travel-to-nearest-belt routing + an ore liquidation sink. Deferred — finish marketbots first.
- **NEXT:** (1) user finishes refit + confirms procyon → I redeploy mb fleet (recovers fuel + asteroid/gas/ice harvest); (2) measure cycle durations → build captures-first boundary-gate + shorten mine count; (3) revive ship_listings; (4) later: mining pool from miner-*.

### ✅ RESOLVED 2026-07-01 — marketbots fully recovered to MARKET-ONLY
All 16 stranded bots recovered (user: 5k/agent faction gift + manual tow of the 11 empty + fly-home of the 5 fueled; I had to STOP the mb overmind first — the worker's auto-reconnect kept stealing the session back, the `session_replaced` tug-of-war). Relaunched full 34 on idle_market (overmind pid 2783288). Post-check 10:37Z: **34/34 up, healthy, ALL DOCKED, 0 stranded, 0 capture failures, market_orders freshest = live.** Mining stays OFF → no re-stranding. Marketbot saga CLOSED.
- **TRADER/HAUL fleet has the SAME fuel-strand edge (2026-07-01):** haul fleet (10 trader-* + 10 salvager-* + hauler on haul.sock, pid 2572334, 1d18h up, 21/21 healthy, 0 restarts) is very profitable — **510 trades / 8.86M realized profit last 24h**, total credits 13.98M.
  - **trader-8: RECOVERED** — user gifted credits (→14,160 cr), it refueled 420/420 and resumed trading. ✅
  - **trader-9 (@ Iron Reach/rubys_vigil) + salvager-3 (@ The Anvil/steelpoint): STUCK, PARKED (user decision 2026-07-01 = leave until next haul restart).** Both 0 fuel at a **non-base POI** with plenty of credits (233k/376k) — the SAME chicken-egg as marketbots: can't dock (no base), can't travel (no fuel), credits useless. Do NOT self-heal. **TODO at next planned haul-fleet restart: stop fleet → tow trader-9 + salvager-3 to a base → relaunch.** (Not worth halting 21 profitable traders now for 2 bots.) Salvagers are most exposed (work at non-base wreck sites).
  - Durable fix for BOTH fleets: a fuel-guard / auto-recovery (burn fuel_cells via autopilotUseFuelCells, or refuse to leave a base without round-trip fuel). Worth building; not urgent.
- **HAUL FLEET SHUT DOWN 2026-07-01 ~12:43 local** (user: wind down to manually recover stuck traders). Graceful drain first (SIGUSR1 → 16/21 finished sells cleanly; 5 stalled [salvager-2/6/7/8, trader-2] force-stopped via SIGTERM with cargo safe in-hold — will sell on relaunch, no loss). Overmind (was 2572334) DOWN, haul.sock removed, all sessions released. **Recovery TODO (sessions now free): TOW trader-9 (Iron Reach/rubys_vigil), salvager-3 (The Anvil/steelpoint), trader-4 (Market Prime/the_beacon, only 2120cr→also gift); trader-5 (2 fuel, funded → may self-limp); trader-6 (docked-at-base, 828k cr → self-recovers on relaunch).** Relaunch line = the standard haul overmind cmd (--socket haul.sock --fleet <haul roster> --status-file fleet-status.json --history-file haul-history.jsonl); see [[reference_overmind_launch_commands]].
- **CAPACITY FINDING 2026-07-01 — haulers are CARGO-CONSTRAINED (upgrades warranted).** Joined haul_results→arbitrage_opportunities: **73% of trades leave depth on the table, avg fill fraction 0.49 (capture <half the opportunity), ~1305 units/trade left unbought.** Profit scales with hull cargo: big holds (trader-2=480, salvager-8=400 cap) earn 29–34k/trade; small holds (60–100 cap: trader-3/7/10, salvager-1/2/4/5/6/7/9, hauler-0) earn 10–13k. Fleet ~510 trades/9.4M per day → lifting small ships toward full capture ≈ +2–3M/day. FIX = cargo_expander modules (cargo_expander_iii scarce) and/or swap small hulls for high-cargo; use cmd/tools/fit to spec. Ideal to do while fleet is down. PENDING user go-ahead.
- **NEW SERVER EVENTS handled 2026-07-01 (uncommitted, built+lint-clean):** `faction_alliance_proposal` + `faction_alliance_formed` added to protocol/messages.go + client.go handleResponse() + pushOnlyResponseTypes set (space-padded-tag trim). Crafting Collective [CRFT] proposed an alliance; Data Bots [DB] accepted ours. **OPEN: whether to accept CRFT (`faction_accept_ally`) = user decision.** Alliance event family may have more members (rejected/dissolved) — add as they surface, don't guess. [[project_faction_events]]

### ⚠️ OVERNIGHT 2026-06-29→30 RESULT — FUEL-STRAND found (relaunch 00:43 → checked 17:28Z)
- **NO crashes:** all 3 overminds + prune daemon up; mb 34/34 healthy, **0 restarts**. 2,333 update_market captures, 10,143 deposit cycles overnight. market.db steady 5.6G (4h prune working). Haul 21 healthy 11.48M. Shuttle johnny_cab earned ~590k overnight → **1.37M**.
- **Harvest concept PROVEN:** the 3 refitted bots gathered real cargo (alpha_centauri 312 ice, cargo_lanes/deep_range gas, belt bot iron_reach ore).
- **BUT the mine/harvest loop's FUEL ECONOMICS are too tight.** Round-trip (station→belt/cloud→back) ≈ the whole tank (marketbot tanks 70–130). Bots limp home at ~0; refuel-first tops up, they burn it again → knife-edge. **28/34 sit at fuel=0 at any snapshot.** A real fraction STRAND undocked at the resource POI at 0 fuel (haven@belt, krynn@belt, sirius@gas_pocket) — at 0 fuel+undocked they can't travel back to dock → **stop capturing** (mission-killing). Re-sample = MIX: some re-dock (blood_forge, first_step), some stuck. refuel-first only saves bots that make it home; CANNOT prevent a mid-trip strand.
- **Not-yet-refitted gas/ice bots worsen it:** burn fuel on failed-harvest round-trips (sirius: 0 cargo/0 fuel stuck). Overnight errors: 48,155 "nothing to mine" (depleted belts/failed) + 8,256 "need a gas harvester" + 2,423 "need an ice harvester" + 35 disconnects.
- **ROOT CAUSE deeper than distance = NO MONEY (user 2026-06-30):** bots `deposit_all` mined resources into storage rather than SELL, raw resources fetch ~0 cr, so the fleet is BROKE (median **1 cr**, 30/34 under 500, total 57k) → can't refuel → strand. Confirmed: marketbots are **NOT in a faction** (faction_id=None) so `faction_withdraw_credits` (exists, client_commands.go:2042) isn't available without joining; **no passive fuel regen** in docs → the stranded bots will NOT self-recover.
- **DECISION (user 2026-06-30): marketbots → MARKET-ONLY, PERMANENT.** Market-watching needs no fuel/credits (a docked station read is free), so mining is decoupled entirely. SHIPPED commit (idle_market.smolt = `dock`+`refuel`; resident/_gas/_ice idle → idle_market; .gitignore negation). Redeployed mb overmind 2715610. Result: **18/34 recovered to DOCKED+capturing for free** (the ones that were undocked at a STATION POI; `dock` = "Already docked (success)" harmless). With mining off, bots never undock again → stay parked + capture. To ever re-enable mining: flip idle back to idle_mine/_gas/_ice (note kept in roles.yaml).
- **16 still stranded at resource POIs (no station to dock at):** user doing **one-time 5,000 cr/agent faction gift + manual TOW recovery**. Split: **11 truly empty (0 fuel/broke, need TOW):** gold_run, haven, market_prime (both @ Haven commerce_fields), krynn, nova_terra, procyon, sirius, sol, the_levy, treasure_cache, void_gate. **5 have fuel (just need fly-home, NO tow):** cargo_lanes(109f/8988cr), deep_range(118/6204), factory_belt(129/2754), iron_reach(79/38216), alpha_centauri(42/6). (offered to nudge the 5 home via one-off `travel`.)
- **OPS GOTCHA — arbitrage scanner has NO input-freshness filter.** `market.ScanOptions` (pkg/market/types.go:219) only has `ExpiresIn` = *opportunity* TTL (default **6h**), no max-age on the SOURCE prices; each scan expires prior `available` rows then regenerates from whatever's in market_orders regardless of age (arbitrage.go:106,174). So when the market-data source (marketbots) is down **>~30 min**, the scanner keeps emitting opportunities off stale prices → haulers scout PHANTOMS (waste fuel/time; NO bad trades — `haulGate` haul.go:98 verifies LIVE liveAsk/sellBid at the station before committing). Haul role has no update_market schedule (roles.yaml `hauler:{idle:haul}`) so haulers DON'T re-freshen market.db. Mitigation during a long mb-down window: keep it <30 min, or pause the haul claim loop until mb is back. Haulers are self-funding + protected → NOT brought down during the 2026-07-01 mb recovery.
- **FUTURE — funded MINING belongs to a dedicated miner-* pool, not marketbots.** Economic framework surfaced: mining only pays if (raw-sale OR craft-output value of a cargo-load) > round-trip fuel cost — measure per-resource/per-system first. Funding models: (A) sell-a-fraction self-fund (likely insufficient — raw=fractions); (B) craft-then-sell (real value, needs recipes/skill/demand — only some resources); (C) treasury stipend via faction_withdraw_credits (needs faction join + withdraw likely location-bound + only sane if mining is net-positive — use as BOOTSTRAP not subsidy). NOT STARTED; gated on the economics measurement.

## ⭐⭐⭐⭐⭐ SESSION 2026-06-29 (SHUTTLE/HAUL FIXES + FACTION-TAG FEATURE)
All on branch `fix/shuttle-targeting` (pushed). See [[project_treasury_and_shuttle]] (shuttle/treasury detail) + [[project_haul_lawless_routing]] (haul detail).
- **Shipped + deployed today:** shuttle deliver-aboard deadlock fix (e4295f6); haul Lawless/station-less recovery+avoidance (ceb1673, RECOVERY VALIDATED IN PROD); stale-docked flag fix (cdd3983, `Docked=st.Doc && !Traveling`); **faction-tag dashboard feature** (0dc8a4f + trim 2a08a29) — worker reports faction_id/tag via heartbeat (self-heal fetches tag mid-run), `:8087` renders `agent (TAG)`, confirmed showing **`johnny_cab (DB)`**.
- **Deploy state:** shuttle overmind + `:8087` dashboard (`bin/overmind-status`, pid ~2622126) restarted on new binaries. **haul overmind** redeployed earlier (~08:44, pid 2572334) for recovery/docked fixes but is on the binary BEFORE the faction feature — haulers are factionless so it doesn't matter for tags. **mb overmind (pid 2541492) NOT touched** (factionless). bin/worker/overmind/overmind-status all rebuilt with the faction feature.
- **KEY FINDING: the ENTIRE haul fleet (21) + all marketbots are FACTIONLESS** → the 5% treasury donation computes-then-silently-skips fleet-wide. johnny_cab (faction **DB**) is the lone member.
- **Treasury through johnny_cab STILL UNCONFIRMED — blocked on the shuttle 0-fresh-boards problem, NOT lockbox.** Post-restart johnny_cab earns (credits climbing) but `fresh_boards=0`; all deliveries are pre-restart-backlog resume-deliveries (bookedFare=0 → no cut by design). Deposit only fires on a FRESH board+deliver. See [[project_treasury_and_shuttle]].
- **NEXT (user deferred to a fresh session 2026-06-29): the capital-camping shuttle fix — NOW VALIDATED LIVE.** A manual play_as session on johnny_cab proved it: docked at Haven capital with free berths, `list_station_passengers` showed **4 rich first/business fares queued** vs the **0** it got at industrial drop-offs — presence at a capital was the whole problem. Fix = after each delivery route to NEAREST EMPIRE CAPITAL (Haven/Sol/Krynn/Frontier/Nexus Prime) + camp; ALSO board all same-route fares (batching ~doubles a run); fix the `list_passengers` aboard-fare-shows-0 display bug; stop deliver-aboard fixation when berths are free. Full validated findings in [[project_treasury_and_shuttle]] (CAPITAL-CAMPING VALIDATED section). SECONDARY: verify `shuttleDeliver` completes long deliveries synchronously (else fresh fares finish via the bookedFare=0 path and skip the cut). Also queued: [[project_haul_lawless_routing]] + [[project_per_death_loss_capture]].
- **OPS: shuttle overmind BACK UP** (pid 2644122, worker 2644132) as of 2026-06-29 ~18:38 — user finished the manual play_as session ("overmind take the wheel") after banking the ~60k Krynn/Blood Forge batch (johnny_cab now 658k cr, at Blood Forge with 9 fares waiting). It's on the current binary (NO capital-camping fix yet), so expect it to wander again until that fix lands. haul (2572334) + mb (2541492) overminds also UP.

## ⭐⭐⭐⭐ SESSION 2026-06-28 (RESTART DEPLOYED)
**The long-pending operator-gated restart is DONE.** Brought both pools down (SIGTERM both old-binary overminds — both predated the 06-27 20:09 drain-binary rebuild, so SIGUSR1 drain was NOT available on the running procs; SIGTERM was the clean path; no orphaned workers), ran the market.db **4h-prune + VACUUM** (writers all stopped), then relaunched everything on the **freshly rebuilt drain binary** (`bin/overmind`+`bin/worker`+`bin/market-prune`, built 06-28 00:05, drain handler confirmed present). So now LIVE: empty-book watchdog + display-name fix + graceful drain + re-added salvager-9/trader-9.
- **market.db: 13G → 5.6G.** First tool-run VACUUM silently failed (DB locked by hidden writers); root cause = besides the overmind pools, **4 non-overmind writers** held market.db: `arbitrage-scanner` + 3 standalone `play_as` (craftsman-1, hauler-1, databot). Per user: stopped ALL play_as (NOT restarted — interactive `go run` sessions the user launches). `arbitrage-scanner` was an orphan from the prior session (started same second as mb-overmind, reparented to systemd --user, no systemd/cron unit) → **restored identically** post-VACUUM: `bin/arbitrage-scanner watch --interval 15m --offset 3m`. DELETE alone left 1.65M free pages (~6.5G); needed checkpoint(TRUNCATE)→VACUUM→checkpoint(TRUNCATE) to actually shrink (WAL-mode VACUUM parks the rebuild in the WAL until a truncating checkpoint).
- **hauler-0 ADDED to the haul pool** (user said it was ready). `data/overmind/haul-fleet.yaml` now **21** (hauler-0 + salvager-1..10 + trader-1..10). hauler-0 = game agent `smt_boss` (outerrim), confirmed connecting.
- **mb-fleet roster now has a STABLE source**: copied the 34-bot roster from the prior session's scratchpad to `data/overmind/mb-fleet.yaml` (was only in `/tmp/.../1e4c8a31/scratchpad/`, fragile). NOTE `data/overmind/fleet.yaml` is a DIFFERENT fleet (residents+explorers), not the marketbots.
- **prune daemon now at 4h** (was 12h): `bin/market-prune --db-path data/market.db --retain 4h --interval 30m`.
- **RELAUNCH RECIPES (all via `setsid nohup ... &` so they survive the session; logs in `data/overmind/*.log`; `--stagger 10s` per fleet to keep combined login rate ~12/min):**
  - mb: `setsid nohup bin/overmind --fleet data/overmind/mb-fleet.yaml --socket data/overmind/mb.sock --worker-bin bin/worker --status-file data/overmind/mb-status.json --history-file data/overmind/mb-history.jsonl --market-db-path data/market.db --stagger 10s > data/overmind/mb-overmind.log 2>&1 &`
  - haul: same w/ `--fleet data/overmind/haul-fleet.yaml --socket data/overmind/haul.sock --status-file data/overmind/fleet-status.json --history-file data/overmind/fleet-history.jsonl --stagger 10s > data/overmind/haul-overmind.log`
- **NOW that the drain binary is LIVE, graceful drain works:** `kill -USR1 <overmind-pid>` to quiesce (USR2 resume, TERM stop) — use this for the NEXT restart instead of SIGTERM. [[project_overmind_graceful_drain]] [[reference_market_db_prune]]
- **NEXT:** (a) watch fleet health as workers finish staggering up (mb 34 @10s ≈ 6min, haul 21 @10s ≈ 3.5min); (b) last mobile role = **salvager**.

## ⭐⭐⭐ SESSION 2026-06-27 (END OF SESSION — START HERE)
Three things shipped + PUSHED to origin/main `1821d9e` (was `a9c90af`):
1. **Empty-book watchdog** `a9c90af` — `haulDestCaptured` catches a captured-but-dead destination (zero buyers), distinct from "no recent capture"; both watchdog entry points rewired. Fixes the Blood-Forge dead-end loop.
2. **System display-name fix** `0cfd605` — `buildStatus`→`displaySystem(st)` prefers `SystemData.Name`; fleet-report stops showing half ids / half names.
3. **Overmind graceful Drain** `784f422..1821d9e` — brainstorm→spec(`b576b26`)→plan(`818b90b`)→5 TDD commits via subagent-driven SDD, every task two-stage-reviewed + opus whole-branch review = Ready-to-merge YES. SIGUSR1 quiesce-and-hold (workers finish current standing-loop pass → report `Status.Drained` → overmind polls `Fleet.DrainProgress`, logs "fleet drained"), SIGUSR2 resume, SIGTERM unchanged stop. [[project_overmind_graceful_drain]]

Also: salvager-9 + trader-9 RECOVERED + re-added to `data/overmind/haul-fleet.yaml` (roster back to 20). market.db prune CORRECTION (it was already running — daemon 12h/30m — not broken).

**THE ONE PENDING ACTION = an operator-gated restart** (no go yet). It deploys everything at once (`bin/worker`+`bin/overmind` already rebuilt, NOT yet live): empty-book + display-name + drain + the re-added 9s, AND — while writers are stopped — the market.db 4h-prune + VACUUM (13GB→~3-4GB; live 12h→4h delete locks writes >2min, proven, so it MUST be downtime). See [[reference_market_db_prune]] for the runbook.
**THEN: the salvager role** (last mobile role; explorer+miner+hauler all shipped).

Live-fleet watch as of last report (06:45Z, volatile — verify live next session): fleet very productive (86 hauls, realized 2.69M, Δ +1.04M). **salvager-3 down to 3 credits** + trader-3 to 3,029 (deep buy legs — salvager-3 risks bricking if it can't refuel/sell). **salvager-10 frozen at 9 hauls / 100,082cr for ~2.5h** (possibly stuck). All 18 heartbeating.

---
*(Older threads below — 2026-06-23 onward.)*

## ⭐⭐ CLAIM WATCHDOG DONE (Steps 1+2) — 2026-06-27, all PUSHED to origin/main `1c4ecad`
- **Full in-flight claim watchdog built + pushed** (7 commits `00c4764`..`1c4ecad`, all TDD, build+race+golangci-lint green). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-27-claim-watchdog*`. Fixes the live trader-5/trader-9 stuck-sell loop (hauler arrives into evaporated demand → loops `sell` forever).
  - **Step 1 (on-arrival safety net, LIVE):** `pkg/worker/watchdog.go` `absorbableProceeds`+`arrivalDecision` (break-even over live buy book, 10% loss tolerance); `OpportunityStore.GetStationOrders`; wired into `haulSellLeg` before the sell — thin demand → `create_sell_order` @cost + complete claim (not dump). Absent order data falls through to normal sell (freshness rule).
  - **Step 2 (per-jump re-route, LIVE):** `autopilot.go` `WaypointCheck`+`errAutopilotStopped` early-stop (additive, sell-leg only); `bestReroute` finder + `haulFindReroute` (graph off `deps.KB`, `FindBestPrices`+`BFSJumps`); `haulSellLeg` re-route loop, **1 re-route max** then ride it out.
- **DEPLOY STATE: Steps 1 AND 2 both LIVE** as of 2026-06-28 ~00:48 local — rebuilt `bin/worker` (Step 2 folded in) + restarted haul overmind on an **18-agent roster** (salvager-9 + trader-9 PARKED — commented out in `data/overmind/haul-fleet.yaml`, backup `haul-fleet.yaml.bak-20260628`). Relaunch recipe: `nohup ./bin/overmind --fleet data/overmind/haul-fleet.yaml --socket data/overmind/haul.sock --worker-bin bin/worker --status-file data/overmind/fleet-status.json --history-file data/overmind/fleet-history.jsonl --market-db-path data/market.db >> data/overmind/logs/haul-overmind.log 2>&1 &` (stop+relaunch = SEPARATE bash commands; overmind sends Abort then exits immediately — wait for haul-socket workers to exit before relaunch).
- **fleet-report tweak** (`2c7139d`): START BAL before CREDITS; TOTALS row now sums START BAL + REALIZED P.
- **salvager-9 + trader-9 RECOVERED + re-added to `data/overmind/haul-fleet.yaml` 2026-06-27** (user refueled them + sold their cargo manually at another station). Roster back to **20**; takes effect on the next overmind restart (roster loads at Run() start — NOT auto-applied to the running fleet). Dynamic single-worker exit/join (vs full restart) saved as [[project_fleet_pool_dynamic_membership]].
- **PENDING RESTART will deploy (operator-gated, NO go yet):** empty-book watchdog (`a9c90af`) + system display-name fix (`0cfd605`) — BOTH need a `bin/worker` rebuild — + the re-added salvager-9/trader-9. Drain adds a 4th once implemented; batch them.
- **EMPTY-BOOK WATCHDOG FOLLOW-UP DONE + pushed `a9c90af`**: `haulDestCaptured` distinguishes "captured station, zero buyers" (dead demand → cost-order/re-route) from "no recent capture" (freshness → sell normally) via a station-wide `GetStationOrders(station,"")` read when the item book is empty. Both entry points rewired.
- **SYSTEM DISPLAY-NAME FIX `0cfd605`**: `buildStatus`→`displaySystem(st)` prefers `SystemData.Name` (matched to current system) so fleet-report no longer shows half ids / half names. Cosmetic; needs `bin/worker` rebuild.
- **DRAIN BUILT + merge-ready (5 TDD commits `784f422`..`1821d9e`, NOT pushed)** via subagent-driven SDD — per-task two-stage reviews all clean + opus whole-branch review = Ready-to-merge YES (no Critical/Important; full state-machine + concurrency + dead-worker/straggler edges verified). Independent fleet drain state: **SIGUSR1** broadcasts `control.TypeDrain` → workers finish current standing-loop pass then hold + report `Status.Drained` → overmind polls `Fleet.DrainProgress` (Healthy-gated), logs "fleet drained — safe to stop", HOLDS; **SIGUSR2** resume; **SIGTERM** unchanged stop. Files: `control` wire (TypeDrain+Status.Drained), `pkg/worker/standing.go` (Draining gate + SetDrained), `cmd/worker/main.go` (draining/drained atomics), `pkg/overmind/supervisor/fleet.go` (DrainProgress), `cmd/overmind/{drain.go,main.go}` (broadcast/drainComplete + signal loop). Spec `b576b26`, plan `818b90b`. `bin/worker`+`bin/overmind` rebuilt but NOT deployed. [[project_overmind_graceful_drain]]
- **PUSHED to origin/main `1821d9e` 2026-06-27** (`a9c90af..1821d9e`, 8 commits: spec `b576b26` + display-fix `0cfd605` + plan `818b90b` + 5 drain commits). Working tree clean except the user's long-standing unrelated WIP (cmd/auto-*, play_as, pkg/agent/runner.go, pkg/worker/capture.go, data/agents/*) + untracked marketbot dirs — all untouched.
- **NEXT:** (a) operator-gated restart to deploy (empty-book + display-name + drain + re-added salvager-9/trader-9 — all batch into ONE `bin/worker`+`bin/overmind` restart; both binaries already rebuilt) — AND while writers are stopped, do the market.db 4h-prune + VACUUM (13GB→~3-4GB; see [[reference_market_db_prune]]); (b) last mobile role = **salvager**. Fleet = 18 haulers on haul.sock + 34 marketbots on mb.sock. To drain on the live fleet once deployed: `kill -USR1 <overmind-pid>` (resume USR2, stop TERM).
- **market.db prune CORRECTION:** pruning was ALREADY running (daemon `--retain 12h --interval 30m`, found via process list not cron); 13GB = stable 12h firehose, not runaway growth. User wants 4h (~3-4GB) — deferred to the restart because a live 12h→4h backlog DELETE locks writes >2min (proven). [[reference_market_db_prune]]

## ⭐⭐ OVERNIGHT RUN CONCLUSION (2026-06-26 ~13:20 UTC): FLEET IS FUEL-LOCKED — build fuel-aware routing next
- **18-hauler fleet ran ~6.5h on the automated pipeline (half_hourly capture → auto-scan watch → gated hauls).** Result: front-loaded profit then ground to a near-halt on fuel starvation. 123 completed hauls; fleet liquid Δ peaked **+247k** (banked, real); realized metric 22.9M but **~11-12M real** after stripping TWO uncapped-quantity phantoms (salvager-4 opp 4001 silicon_ore 45,080-qty = 9.92M fake; trader-2 ~810k fake — both `gross_profit` computed on full order qty, NOT ship cargo cap).
- **THE binding constraint = FUEL.** Late-run log: **168 `Insufficient fuel for travel/jump` events vs 0 completes** per ~600 haul-engine lines; haul rate collapsed +8→+1→+1 per 30-min window. Haulers pick opps they can't fuel-reach → release → retry → churn. The existing autopilot tactical refuel (`ensureRouteFuel`, merged `3f5c365`) only refuels AT A BASE at route start; mid-route or no-base POIs strand. salvager-3 BRICKED (Unknown Edge/edgelight, 2cr, insufficient fuel, no base — needs manual credit gift + relocate; same terminal case trader-4 hit before).
- **FIX = fuel-aware routing** (the deferred B/C from [[project_overmind_fleet_manager]]): (1) reject/penalize routes whose total fuel need exceeds tank in `RankHaulOpportunities`; (2) refuel at INTERMEDIATE stations en route, not just at start. Secondary: cargo-capacity cap + outlier guard on scanner `gross_profit` so the realized metric stops getting phantom-polluted. A restart only re-fuels temporarily (re-depletes in ~1h) — needs the code fix.
- **What WORKED & is proven:** half_hourly `update_market` + `arbitrage-scanner watch` (auto-scan every 30m at :05/:35, 0 manual intervention all night) + `completed_at` durations (local 0-jump hauls ~2-5min, multi-jump ~9-14min) + reconnect gate (0 real rate-limit blocks all night) + balance tracking. salvager-2 top performer (14 hauls, +340k liquid, 1.66M realized). Productive core ~12 carries ~6 chronically idle/stuck (fuel + synchrony clustering: trader-1/3 idle-parked at synchrony_hub despite healthy balances).
- Fleet still running detached (haul.sock 18 haulers incl. live-added salvager-7 (unsupervised) + salvager-5/6; mb.sock 43 marketbots; watch). Reports via session cron.

## ⭐ PIPELINE AUTOMATION + DURATION INSTRUMENTATION (2026-06-26, committed LOCAL, NOT pushed)
- **completed_at instrumentation** (commit `72a4d7e`): `arbitrage_opportunities.completed_at` column (schema.sql + idempotent `ensureColumn` ALTER in `pkg/market/migrations.go` for existing DBs — live data/market.db migrated, col idx 20); `CompleteOpportunity` stamps RFC3339; surfaced via shared SELECT/scan + `ArbitrageOpportunity.CompletedAt`. LIVE-CONFIRMED stamping. **Why:** real per-haul duration (completed_at − claimed_at) replaces the noisy claim-to-next-claim proxy; feeds future net-$/minute selection scoring. First few post-restart completions span the restart so durations are inflated — clean ones need claimed_at > the 2026-06-26T06:53Z restart.
- **half_hourly + scan automation** (commit `e268cd5`): added `half_hourly` to `pkg/worker` scheduler (`CurrentBoundary`/`NextBoundary` align :00/:30 UTC, `ValidFrequencies`). NEW `arbitrage-scanner watch` subcommand (`cmd/arbitrage-scanner/watch.go`): boundary-aligned recurring scan, default every 30m at boundary+5m, one collector open while fleet claims/completes via WAL. **This automates the previously-MANUAL scan** — the real unblock (marketbots captured hourly but the opportunity pool only refreshed when I ran the scanner by hand → pool was hours stale).
- **Decay hypothesis (user):** opps computed from our snapshots go stale over the hour (prices drift + external players/NPCs deplete sell orders; our `claimed` flag only coordinates OUR fleet) → late-hour hauls net little/loss. **Caveat verified SAFE:** scan expire is `WHERE status='available'` ONLY — claimed/completed (in-flight) trips untouched, so frequent re-scans can't erase a running trip. Plan order (user-approved): automate scan → half_hourly marketbots → watch completed_at decay curve → tune cadence (30→15 only if data shows decay inside 30m).
- **DEPLOYED LIVE 2026-06-26T06:53Z**: full fleet restarted on rebuilt binaries. Marketbots: all 43 `update_market`→`half_hourly` (view_market + kb_update stay hourly; `data/agents/marketbot_*/schedule.json` edited via jq while stopped). First half_hourly capture 07:00Z, first auto-scan 07:05Z. **HAULERS NOW 17**: added salvager-5 + salvager-6 (user) → trader-1..10 + salvager-1,2,3,4,5,6,10. Roster `scratchpad/haul-fleet-17.yaml`. 0 rate-limit errors on restart (gate held; initial logins paced by overmind 5s stagger — NOTE: gate covers RECONNECTS only, not first logins, so still stagger fleet launches).
- **Watch process**: `nohup ./bin/arbitrage-scanner watch` (log scratchpad/scanner-watch.log). Overminds: haul.sock (fleet-status.json) + mb.sock (mb-status.json). Stop fleet: `pkill -x overmind` + `pkill -x worker` (NOT `pkill -f` — self-matches the shell whose cmdline contains the pattern → kills your own bash, exit 144).
- **NOT pushed**: origin/main still `af14269`; local main now +`72a4d7e`+`e268cd5`. Push when ready.

## Fleet balance tracking + reporting (built 2026-06-25, committed LOCAL `69ebe37`, NOT pushed)
- `pkg/overmind/balances`: Recorder writes `data/overmind/fleet-status.json` every overmind tick (live per-worker view) + appends `data/overmind/fleet-history.jsonl` — one row per agent per UTC day (first row = STARTING BALANCE; midnight-past-tick captures daily balances for deltas). `Seen` requires a real Status heartbeat (Status.Timestamp), NOT just Hello — else credits log a bogus 0 (this bit us live; fixed).
- `cmd/tools/fleet-report` (build to `bin/fleet-report`): read-only; joins status file + history + realized profit (completed opps from market.db) → per-agent table: credits, start bal, total Δ, since-00:00 Δ, hauls, realized profit. LIVE-VALIDATED (trader-1 start 319,367, trader-2 116,526 captured correctly).
- Wired into `cmd/overmind/main.go` ticker (`--status-file`/`--history-file` flags). Snapshot owner = overmind (user's choice, reliable/always-on).
- **30-min periodic report**: session-only cron `7f87da3a` (`13,43 * * * *`) wakes me to run fleet-report + present. Dies on session end, 7-day auto-expire.
- **HAULER POOL EXPANDED to 15**: trader-1..10 + salvager-1,2,3,4,10 (salvager-1/2 have larger/faster ships + seed money), all role hauler. Current roster `scratchpad/haul-fleet-15.yaml`; overmind relaunched on it (`--fleet haul-fleet-15.yaml --status-file data/overmind/fleet-status.json --history-file data/overmind/fleet-history.jsonl`). Anyone can haul. Roster lives in scratchpad — move to data/overmind/ + commit if it should survive a fresh session.
- Launch recipe MUST be its OWN bash command (kill+relaunch in one command repeatedly fails/exits-1 mid-script in this shell — stop in one call, start in the next).

## Done this session (2026-06-23, all merged + PUSHED to origin/main, build+test+lint green)
- **Travel/jump timeout bugfix** — GameClock forward-drift made arrival waits collapse below real travel time → false mid-journey timeouts. Fixed via `arrivalWaitTimeout` floor/cap in `pkg/game/client.go` (merge `37fff27`). See [[reference_gameclock_forward_drift]].
- **Overmind #1 supervisor hardening** — double-spawn fix + staggered startup (merge `4b745ea`). Detail in [[project_overmind_fleet_manager]].
- **Overmind #2 autopilot/routing extraction** — new `pkg/navigation` + `pkg/worker/autopilot.go`; WorkerDispatch jump/autopilot; play_as thin wrappers. Mobile-worker navigation prerequisite. Full detail in [[project_overmind_fleet_manager]].
- **Scheduler dedup** — `Scheduler.Add` rejects same-frequency+command duplicates (commit `99c3eec`).
- **Login rate-limit probe + restart pacing** (merge `80e580e`) — `cmd/debug/login-probe` (burst/paced harness) measured the per-IP throttle: onset ~25/min, connect-layer stall (not `ip_timed_out`), recovery <1min, 5s stagger validated with ~2x headroom. Added `Supervisor.RestartBatch` (default 1/reap-tick ≈ 12/min) + `--restart-batch` so mass restarts stay paced. See [[reference_login_rate_limits]].

(Prior session: Market Data Consolidation fully landed on origin/main — [[project_market_intelligence]].)

## Also done this session (2026-06-23, MERGED + PUSHED to origin/main)
- **Overmind Phase 2a — mobile explorer role** (no-ff merge `e881741`, pushed `1ee6f9f..e881741`; branch deleted; 3 TDD tasks + two-stage review + Opus whole-branch review; full `go test ./...` green, 37 pkgs). First mobile role: `pkg/worker/explore.go` (`NextExploreTarget` frontier-first BFS selection + `Explore` engine reusing `Autopilot`), `explore`/`scan` dispatch, explore.smolt script, explorer role + 12-explorer roster. Detail in [[project_overmind_fleet_manager]].
- **Explore id-vs-name bugfix** (`888e651`, pushed e881741..888e651, direct to main): Explore selected on `state.CurrentSystem` (display name "Haven") but the KB graph/systems key on lowercase ids ("haven") → explorers permanently idled "no frontier reachable". Fixed to `state.System.ID`. Found by running the swarm live. See [[reference_exploration_is_content_survey]].
- **SWARM RAN LIVE 2026-06-23**: launched overmind with 12 explorers + 43 marketbot residents (55 agents, staggered 5s, all healthy, ~1 transient restart). Aged all 505 systems' `last_updated_tick` in `data/spacemolt-knowledge.db` to force the stale path; after the id/name fix all 12 explorers began multi-jump autopiloting to stale systems and surveying. Live launch roster = scratch `fleet.swarm.yaml` (canonical `data/overmind/fleet.yaml` still has 4 credential-less stubs: resident-nebula-1/2 + explorer-9/10 had no creds at first but explorer-9/10 now provisioned (outerrim)). Monitor via overmind stdout log (fleet-status table); stop with `pkill -x overmind`.

## Phase 2b DONE + LIVE-VALIDATED + PUSHED 2026-06-24 (`origin/main` @ `b746d57`)
- **Overmind Phase 2b — assigned-task path + directed mining run** (no-ff merge `b3d8121` into main; branch `feat/overmind-assigned-tasks` deleted; 7 TDD tasks + per-task two-stage review + Opus whole-branch review; full `go test ./...` green). Overmind loads `data/overmind/tasks.yaml`, assigns each pending task to an eligible idle worker over the control channel each status tick (`tasks.Store.AssignPending`), records `task_done`/`task_failed` events (`HandleEvent`). Worker runs the task script ONCE under ExecMu (`SubstituteParams` `$KEY$` → `ResolveTokens` live tokens), reports result, resumes idle. NEW `control.Assign` (TypeAssign); NEW `pkg/overmind/tasks` (Task model + LoadTasks + Store/Sender); task-aware `RunStanding` (`NextTask`/`OnTaskResult`/`runTask`, `runLine` now returns error); `cmd/worker` TypeAssign + atomic pendingTask/activeTaskID + live `Status.ActiveTaskID`; `cmd/overmind` `--tasks` flag + AssignPending in ticker. Config: `data/scripts/mining_run.smolt`, `miner: {}` role (idle falls back to get_status), `tasks.yaml` empty default. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-24-overmind-assigned-tasks*`. Full detail in [[project_overmind_fleet_manager]].
  - **Final-review CRITICAL caught + fixed** (`d33b8b1`): worker-death reconcile keyed on absent-from-snapshot, but `Fleet` is ADD-ONLY (`MarkRestart` keeps the entry, sets Healthy=false) → reconcile never fired, task stuck `assigned` forever. Now reverts on `!ok || !w.Healthy`. The Task-4 test passed only because it fed a snapshot shape `Fleet.Snapshot()` never produces — beware hand-built fleet snapshots in tests. Also: task ids reject `:` (HandleEvent splits Detail on first `:`); drift guard now task-driven (covers tasks.yaml scripts; empty seed today so mining_run.smolt not yet walked but its 5 commands are independently dispatchable).
- **PUSHED to origin/main 2026-06-24**: `888e651..b3d8121` (Phase 2b merge) then `b3d8121..b746d57` (mine_local.smolt + 10 miners in fleet). origin/main now at `b746d57`. Working tree clean (only untracked per-agent `schedule.json` runtime files in data/agents/, harmless).
- **LIVE-VALIDATED 2026-06-24**: ran overmind w/ 10 miners + 3 scratch local-mine tasks — pin (mine-test-pin→miner-1) + role assignment (miner-2/3) + real mining (iron/nickel/silicon/copper/sol_alloy ore + antimatter cell → storage) + `task_done` round-trip all confirmed; clean shutdown. Launch recipe: `./bin/overmind --fleet <miners.yaml> --tasks <tasks.yaml> --socket data/overmind/X.sock --worker-bin bin/worker` (SHORT socket path — ~108-char sun_path limit, scratchpad path overflows). Stop: `pkill -x overmind` or `kill <pid>` (both hit the graceful SIGINT/SIGTERM path → workers checkpoint + exit). No `overmind stop` subcommand exists (signal-only; could add later).

## Hauler role DONE 2026-06-24 (= Market Intelligence Phase 5) — merged LOCAL main `5e27734`, NOT pushed
- Autonomous standing role consuming the Phase-4 arbitrage scanner (rank→claim→haul buy→sell→complete). Full SDD with reviews. Detail + spec-approved deferrals + the first-run docking watch-item in [[project_overmind_fleet_manager]]. Local main now ahead of origin/main `c0840bb`; push when ready. Working tree still has the user's unrelated WIP (untouched; stashed/popped around the merge).

## Hauler LIVE-VALIDATED 2026-06-24 (2 runs, trader-1..10) + name-resolution fix merged `fb41ba8`
- Run 2 (10 empty-hold haulers): 0 name-unresolved abandonments, 1 full COMPLETE, auto-dock confirmed for buy+sell. Full detail + the buildNameToID fix + 2 open follow-ups in [[project_overmind_fleet_manager]]. Local main now `fb41ba8` (feature `5e27734` + fix merge), still NOT pushed.

## Hauler PRE-BUY PROFIT GATE built + LIVE-VALIDATED + MERGED LOCAL `7f13cae` 2026-06-25 (NOT pushed)
- Merged no-ff into local main `7f13cae` (branch `feat/hauler-prebuy-gate` deleted). 2 commits `ca72074` (gate) + `45aa1fb` (dock-before-recapture fix). Spec `docs/superpowers/specs/2026-06-25-hauler-prebuy-profit-gate.md`. Build/race-test/lint green.
- **What it does:** before buying, dock at buy station → live `view_market`+`CaptureFromClient` recapture (cadence-independent) → `GetItemStationPrices` for live ask + latest sell bid → `haulGate` requires BOTH margin≥3% AND net≥1000cr or skip (row left claimed). New `haulGate` pure fn (6 table cases); `OpportunityStore.GetItemStationPrices`; nil-safe `HaulDeps.RecaptureBuyMarket`. The dock is REQUIRED — view_market (unlike buy/sell) does not auto-dock; without it the recapture silently fails and the gate falls back to STALE prices (this was the bug the 53-agent run exposed).
- **Live result (10 traders):** gate rejected losing trades (`margin=-3.7% net=-4896`, etc.) AND thin-net (`27.7% net=650` < floor) AND phantom (`no live ask` ×5); dock worked (cannot-dock 0); 0 losing trades executed; 0 completes because (a) **72 fuel-starvation transit failures** + (b) fresh prices reveal most current arbitrage rows unprofitable. Resolves the old trader-4 `must be docked` follow-up (same root).
- **OPEN follow-ups:** (1) **hauler refueling** — TOP operational blocker; haul engine has no refuel, traders strand mid-route; (2) residual `item_not_available` ×3 — recapture→buy race, gate leaves claimed (minor); (3) marketbots DON'T capture at startup (hourly boundary only) → validates the deferred **on-demand `update_market` to marketbot_<system>** idea; (4) deferred **selection scoring** (net $/jump + freshness decay).

## Autopilot TACTICAL STATION-REFUEL built + LIVE-VALIDATED + MERGED LOCAL `3f5c365` 2026-06-25 (NOT pushed)
- `pkg/worker/autopilot.go`: before the jump loop, `ensureRouteFuel` station-refuels when `needsRefuelForRoute` (jump-estimate shortfall OR tank below `AutopilotRefuelThreshold=0.5`) — dock-if-needed + `Refuel()` (full tank). Best-effort: not-at-station → log + proceed. Shared by ALL mobile roles via Autopilot. Pure `needsRefuelForRoute` + `ensureRouteFuel` unit-tested (fakeClient gained `dockErr`). Spec `docs/superpowers/specs/2026-06-25-autopilot-station-refuel.md`. Scope = tactical A only; **fuel-price capture (B) + fuel-aware routing (C) DEFERRED** (full fuel design in [[project_overmind_fleet_manager]]).
- **Live result:** stranding 6+→1; 9/10 traders fuel-healthy, 3 refuels. Lone holdout trader-4 BRICKED (stranded non-station POI + 905 credits — needs manual recovery, can't self-refuel). **0 completes is correct**: gate + refuel both work; remaining blocker = OPPORTUNITY QUALITY (current arbitrage rows mostly thin/inverted/phantom priced fresh) → the deferred freshness/scanner/on-demand-refresh work.
- **Local main far ahead of origin (all unpushed):** `5e27734` hauler + `fb41ba8` name-fix + `7f13cae` gate + `3f5c365` refuel.

## ROOT CAUSE of 0-completes found 2026-06-25 (market.db analysis): EXPIRED opps never retired
- NOT capital (312/313 opps cost ≤500k to buy full qty; avg cost 25k; min-for-1000-gross 53cr), NOT thresholds (311/313 pass BOTH 3%/1000 on snapshot; margins 10–50%+, 124 over 50%; zero high-margin-low-net cases).
- ALL 313 'available' arbitrage_opportunities are ONE scan from 2026-06-24T22:02 (~21h old), expires_at 2026-06-25T04:02 — **all 313 PAST EXPIRY but still status='available'** (expiry not enforced → no re-scan; hauler scan-when-empty never fires because table isn't empty).
- Raw price data IS fresh (`market_orders` 2.1M rows, captures through last hours). So gate re-prices stale opps live → spreads gone/inverted → rejects all → 0 completes. **Fix = retire expired opps + re-scan against fresh market_orders** (scanner/market-pipeline; hauler could also treat all-expired as empty to trigger ScanArbitrage).

## CORRECT MARKET-INTELLIGENCE PIPELINE ORDER (user, 2026-06-25) — the real unblock for completes
- Sequence: **marketbots (resident) run a full capture cycle (fresh update_market snapshots at all stations) → THEN run `cmd/arbitrage-scanner scan` → THEN haulers**. `arbitrage-scanner scan` expires old `available` + inserts fresh (`res.Expired`/`res.Inserted`); flags: --min-profit 1000, --min-price 10, --expires 6h, --limit 500. Built to `bin/arbitrage-scanner`. Later market-intel phases will AUTOMATE this (marketbots fetch → run arbitrage tool after a cycle).
- **Why my 2026-06-25 capstone got 0 completes:** ran the scan WITHOUT a fresh marketbot cycle first, so it scanned partially-stale `market_orders` → produced phantom opps (lingering sell orders: Plasma Injector ×3, Liquid Hydrogen) that fail `item_not_available` live (gate leaves claimed; no loss). Marketbots were NOT in the hauler fleet (9 traders only). Marketbots only capture on the hourly boundary, NOT at startup (so a "full cycle" needs the boundary or an on-demand update_market trigger).
- Gate + refuel both confirmed working this run (0 fuel strands, gate skips phantom via `no live ask`; long profit-dominant routes — one 21-jump — show the deferred net-$/jump scoring is wanted).

## OVERMIND CONTROL-PLANE FEATURE REQUESTS (user, 2026-06-25) — both build on Phase-2b assign_task (single-worker targeting over the unix socket)
1. **Dynamic fleet add/remove** — add/remove an agent from a RUNNING fleet without start/stop, for agents needing human resolution (e.g. trader-4 bricked: stranded non-station + 905cr; workaround was editing fleet.yaml + relaunch). trader-4 since refueled by user.
2. **Broadcast / group commands** — send a command to EVERYONE ("everyone autopilot to sol") or a GROUP ("group:miner switch to gas harvesting", "group:marketbots update_market now"). Grouping by role (fleet already has per-worker role) or named groups. **`group:marketbots update_market now` IS the on-demand refresh** that unblocks the fresh-data pipeline (marketbot cycle → scan → haul) WITHOUT waiting for the hourly boundary — high-value, directly enables profitable-haul completes on demand. See [[project_overmind_fleet_manager]].
3. **Auto-recall** — broadcast "every agent return to NEAREST station and dock". Specific high-value broadcast: gets the fleet to a known docked state. Directly relevant because `update_market` REQUIRES docking (view_market does not auto-dock), so a marketbot left away from a station by its idle behavior captures nothing at the boundary; auto-recall before a capture cycle guarantees all docked. Also a general safe-state / pre-maintenance primitive.

## HAULER ROBUSTNESS ROUND (2026-06-25, built in working tree on main, NOT committed) — pkg/worker + pkg/market
- **Distance cap**: `RankHaulOpportunities(..., maxJumps)` drops buy stations >`DefaultHaulMaxJumps`(3) jumps from current; `DefaultHaulPoolLimit` raised 50→500 so nearby-lower-gross opps are in the pool (GetOpportunities is gross DESC). `HaulDeps.MaxJumps` (0→default). VERIFIED tight for the clustered 10-trader fleet → mass "no opportunities within 3 jumps; idling" → widen to 4-5 OR spread more haulers (user's "anyone can haul" idea).
- **release-on-abandon**: pre-buy abandons call new `market.ReleaseOpportunity` (status→available, owner-only) via `abandonClaim`; post-buy abandons KEEP claim for resume. VERIFIED live: claimed holds ~8 (was leaking to 300+).
- **resume**: `Haul` calls new `market.GetClaimedByAgent` first; if the agent holds a claim, `runClaimedHaul` resumes it (skips buy leg when goods already aboard → `haulSellLeg`). Tested; fires on restart-mid-haul.
- market.db: `ReleaseOpportunity` + `GetClaimedByAgent` (shared `arbitrageSelectJoin` const + `scanOpportunityRows`). Worker `OpportunityStore` gained both. Tests: TestReleaseAndGetClaimedByAgent (market); TestHaulResumesHeldClaimBeforeClaiming + TestRunClaimedHaulResumesWithGoodsAboard + TestRankDistanceCapDropsFarOpps (worker). Full `go test ./...` green, golangci-lint 0. bin/worker rebuilt.
- **CAPSTONE PROVEN** (fresh data, before this round): 3 real profitable completes — trader-3 processing_core 135k, trader-9 silver_ore 120k, trader-6 thruster_nozzle 11.5k gross.
- Cleared 323 leaked claims (UPDATE→'expired' WHERE status='claimed').

## CARGO LIQUIDATION built + committed `af14269` 2026-06-25 (local main) + FLEET RECOVERED
- `liquidateCargo` (pkg/worker/haul.go): with no active claim, before hauling, clears leftover cargo. Docks (skips if already docked — IsDocked guard; "Already docked" was aborting it), recaptures live, per non-reserved item either Sells into a local buy order OR posts a `create_sell_order` at 10% below `bestAskAnywhere` (global best ask, not just local — fixes silver_ore "no local market" stuck). `fuel_cell` reserved. Tested (TestLiquidateCargo, 6 subtests). LIVE-VALIDATED: sell orders posting (cobalt_ore, autocannon_ii, uranium_rod…), resume fired (5 live resumes), claimed holds ~4 (no leak), **9 completed hauls** accumulated.
- Cap widened 3→5 (`DefaultHaulMaxJumps=5`), committed `8ed80a2`.
- **PUSHED to origin/main 2026-06-25**: `c0840bb..af14269` — 20 commits, the ENTIRE hauler/market-intel body (hauler role 5e27734 → name-fix fb41ba8 → gate 7f13cae → refuel 3f5c365 → cap+release+resume 8ed80a2 → liquidation af14269). origin/main now at `af14269`. (User's unrelated WIP: cmd/*, pkg/worker/capture.go, data/agents/* still uncommitted/local — untouched.)
- **User gifted 100k credits to each trader-*** → broke ones recovered (trader-4=100001, trader-10=110298, trader-2=96708, trader-5=121202). Fleet now recapitalized + productively hauling. Running: `nohup ./bin/overmind --fleet <scratch>/haul-fleet-10.yaml --socket data/overmind/haul.sock --worker-bin bin/worker`; marketbots on mb.sock.
- Known limitation: liquidation only sell-ORDERS non-local items (no cross-station travel-to-buyer "find a market"); a belt-stuck ship can't dock to liquidate. Future: travel to a buyer station for option (a).

## Next action (clear pickup)
- **Decide on the not-yet-committed hauler robustness round** (distance cap + release-on-abandon + resume): commit/merge it; consider widening cap 3→4-5.
- **Recover the degraded fleet**: sell/offload stale cargo (trader-5 et al.), recapitalize broke traders (trader-4=1cr, trader-10=3cr, trader-2=40cr).
- **Full-pipeline capstone:** marketbots capture cycle → `bin/arbitrage-scanner scan` → haulers, to finally see profitable completes. Needs marketbots refreshing (hourly boundary or on-demand update_market trigger) BEFORE the scan. Options the user floated: **fuel-price intelligence (B)**, **fuel-aware routing (C)**, **on-demand `update_market` to marketbot_<system>**, **selection scoring (net $/jump + freshness decay)**, **marketbots capture only at hourly boundary not startup**.
- **Recover/triage stranded+broke trader accounts** (trader-4) before next live profit test.
- **Push local main to origin** when ready (4 unpushed merges above).
- **Prior hauler follow-ups:** residual `item_not_available` race (gate leaves claimed); low-credit workers abandon high-value opps (`qty=0`).
- **Last mobile role: salvager** (explorer + miner + hauler all shipped & validated/merged). Same pattern: role in roles.yaml + script in data/scripts/ (.gitignore-allowlist) + WorkerDispatch coverage if new cmds + agents in fleet.yaml.
- **Push local main** to origin when ready (`5e27734` hauler + `fb41ba8` fix are local-only).
- **Ship module management** (Phase 2b+ cross-role prerequisite, income-gated): buy+install/fit a module then verify — miners want 2nd/3rd mining_laser or gas/ice harvester; explorers want survey_scanner/anomaly_detector. See [[project_overmind_fleet_manager]].
- **api-monitor faction_list drift** (queued): `facility faction_list` sub-action returns action="faction_list", colliding with the top-level faction_list command → diffed against wrong struct. Monitor keys on response action field (`client_api_monitor.go` actionResponseTypes). Benign for now (play_as parses fields for display).

## Not ours
- **Arbitrage detector** (#3) — other team; `arbitrage_opportunities` table exists, no producer yet.
