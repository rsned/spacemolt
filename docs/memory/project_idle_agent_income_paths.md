---
name: project_idle_agent_income_paths
description: Idle-agent income initiative — mission-runner fleet (A) specced 2026-07-16; mining vertical (B) next; combat/bounty (C) deferred pending combat lab
metadata: 
  node_type: memory
  type: project
  originSessionId: 051adb3a-a06c-4dac-aed0-f51137d16814
---

**Initiative (2026-07-16):** ~half of ~160 agent accounts idle (15 pirate, 12 explorer, 10 each trader/salvager/miner/fighter/engineer, 9 random, 5 spark, 5 architect, 2 prophet). Brainstormed income paths from the 8 official guides (spacemolt.com/docs/guides). User picked, in order:

- **A. Mission-runner fleet — SPEC APPROVED + COMMITTED** (`docs/superpowers/specs/2026-07-16-mission-runner-fleet-design.md`, commit `72c1169` on `fix/arbitrage-expiry-filter`). Autonomous runners + telemetry: new `missions` worker task modeled on haul.go; live-board selection (KBUpdateMissions skips procedural missions — the money ones — so KB-driven central scanning is blind); selector gates = type(supply/delivery only)/cargo/expiry/net-cost; stack ≤5 per route; `mission_results` table in pkg/market mirroring haul_results; canary 3–5 idle traders in one empire, T2 freighter, treasury-funded. Phase 2 = exploration circuits (10–20k+/loop, fits explorers) + board-snapshot capture. **BUILT + MERGED TO MAIN 2026-07-16** (main @ `ee8364d`, fast-forward; carried the parent `fix/arbitrage-expiry-filter` commits along; NOT pushed). 7 commits: telemetry (`1edbf7a`), selector (`3009e32`), execution loop (`02a0567`), wiring (`c27070a`+`367ee70`), canary config+runbook (`05d2154`), final-review fixes (`ee8364d`: session re-accept guard, ItemCost=0 on failed buys, Warnings gate). Canary = engineer-1/2 + fighter-1/2 (traders/salvagers ARE the haul fleet). Final review: Ready to merge YES.
**ROADMAP (operator, 2026-07-16 night): overnight soak → morning results review → 4-agent canary → then EVERY idle agent (~80) into the basic mission pool** for baseline XP + credits (completions grant skill_xp_gained: navigation/trading/etc.). Smuggler-onboarding (treasure_cache, 2 chains × 3 missions → smuggling skill + permanent pirate rep/stronghold access) is a phase-2 per-agent capability: [[reference_customs_mechanics]]. Soak infra: one-worker overmind fleet on data/overmind/mission.sock, log+fleet yaml in session scratchpad (mission_results in market.db is the durable record; 6 completions/26,800 cr gross as of 21:45).

**✅ SMOKE PASSED (2026-07-16 ~20:42): first completed mission_results row — engineer-1, Supply Run: Iron Ore, credits_earned=2000.0 [source: action_result], outcome=completed.** Fix commits on main: `ea0c7e5` (active-id resolution via template_id match, storage-first withdraw-then-buy, action-result credits, reposition POIs bug, skip-log dedup), `771cba0` (claim storage-satisfied objectives — server counts storage AT the target base toward delivery via `in_storage`; adrift recovery), `daf09c6` (recover from non-dockable POIs too). `176f18e` (pre-accept availability gate: storage+local-market supply check before accept_mission — stops accept/abandon churn; suspected abandon penalty ~2k cr observed in wallet reconciliation). 3 completions in first hour: iron ore 2000 + Solarian Comforts 3500 (net 3368, bought 12 gold_wiring for 132) + Private Reserve 800 = 6,300 cr gross. NOT pushed. **Smoke ops traps hit:** two overminds on one socket path = the dying one unlinks the live one's socket → worker restart-loop "could not connect to control socket"; kill loops matching own shell cmdline self-kill (use /proc/exe readlink + distinctive patterns). **Remaining polish before canary:** resume-held rows record qty=0/expected_reward=0/from_base empty (backfill from active mission); client State.Credits never updated by complete_mission (heartbeat shows stale wallet until next sync); `[SERVER API CHANGE] get_system has new field "kind"` — serverapi struct update needed; orphan-cargo liquidation + expired outcome still open.

**🌙 OVERNIGHT SOAK RESULTS (2026-07-16 20:35 → 07-17 09:30 local):** worker never crashed/quarantined (2 restarts, both benign); survived a ~6-min LOCAL DNS outage 09:24–09:30 (resolver returned no-such-host for game.spacemolt.com; client reconnect recovered it unaided — the mission loop spams "recovery transit failed … not connected; retry next pass" ~every 5s while disconnected: harmless but a monitor-flooder, worth a connected-gate/backoff before fleet scale-up; monitors must EXCLUDE that pattern). **Economics: earning phase 20:35–23:22 = 6 completed, 26,800 gross / 26,038 net (~8.6k cr/hr), wallet peaked 154,802. Then boards went DRY of plain deliver missions ALL NIGHT — 738 dry board reads, 248 repositions circling 8 stations, zero accepts 23:22→09:30 — and repositioning fuel burned ~2,750 cr/hr, −25.9k overnight, giving back the entire evening's profit (wallet 128.9k by 09:00).** Boards overnight held only v1-rejected types (trade runs, smuggling couriers, workshop production, mining contracts) → mission supply looks diurnal. **#1 pre-canary fix: dry-board backoff — after N dry circuits, PARK at home station and poll cheaply instead of jumping; without it every fleet agent bleeds ~66k/day when boards are empty.** 8 abandoned rows all from the pre-gate churn era (record 0 cost — abandon penalty still unconfirmed in telemetry).

**Smoke iteration 2 (2026-07-16 evening):** first smoke found the requirements-vs-objectives wire bug ([[reference_mission_board_wire_shape]]) AND operator caught a Trade Run targeting Dross Citadel (algol, pirate stronghold). Both fixed in `09cde58` on main (NOT pushed): objectives-based deliverShape (type=delivery + single deliver_item), Warnings gate, stronghold endpoint+route-through guard via buildStrongholdRefs/missionRouteClear, resume via ActiveMissionObjective. Smoke relaunched as one-worker overmind fleet (engineer-1, mission.sock — standalone bin/worker without --socket never runs standing behavior!). Smoke MUST check completed mission_results rows have non-zero credits_earned (delta depends on complete_mission ok-response updating State.Credits within SleepQuick). **Before any scale-up:** orphan-cargo liquidation + `expired` outcome emission (v1 never liquidates expired-mission cargo — watch cargo_used creep). **v2 selector wants:** credit in-cargo/storage stock before buying (buyQty only subtracts mission ProvidedItems); multi-leg chains (First Links = 2 objectives, v1-rejected by design); multi-trip partial delivery for procurement jackpots (6.6M cr class).
- **B. Mining→crafting vertical** — miners feed craft brain, sell refined (2–5x ore) into demand ledger. Own spec AFTER A ships.
- **C. Bounty+salvage** — interesting but combat is a complete unknown; blocked on the automated-combat-testing lab ([[project_spar_testbed]] is the seed) + ship rebuy + per-death loss capture. Risk tolerance: **cheap hulls only** (T1 Axiom 2.5k, insurance always on).
- **D. Base/faction facilities** — rejected as income path for now; heavy capex (200k–3.9M), better later as profit sink.
- Wildlife culls = a mission-board category (fold into A later, doubles as combat data); distress rescues already covered by assist fleet.

**Guide economics:** supply/delivery 1.5–4k cr stackable ×5/route (~5–10k cr/hr); exploration circuits 10–20k+/loop; bounties 2k→15k+ (combat auto-resolves after `attack`); mining 3–5k cr/hr; refined = 2–5x raw.
