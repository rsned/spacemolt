// ICE MINE
SET_MEM "STATION" "grand_exchange"

IF traveling()
  WAIT
ELSE IF cargo_full()
  IF at("grand_exchange")
    DEPOSIT
  ELSE
    MOVE "grand_exchange"
  END
ELSE
  IF at("frostmarket_flats")
    MINE
  ELSE
    MOVE "frostmarket_flats"
  END
END

