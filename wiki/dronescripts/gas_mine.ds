// GAS_MINE
SET_MEM "STATION" "grand_exchange"

IF traveling()
  WAIT
ELSE IF cargo_full()
  IF at("grand_exchange")
    DEPOSIT
  ELSE
    MOVE "grand_exchange"
  END
ELSE
  IF at("trade_winds")
    MINE
  ELSE
    MOVE "trade_winds"
  END
END

